# BTB Mobile Next — `btb next cutover start` batch, 2026-09-02

M15 Mobile Intelligence Foundation paralel şeridinin ilk yerel batch'i.
Dondurulan kapsam: `NXT-OBS-138` / `TASK-0040`, `NXT-OBS-139` / `TASK-0045`,
`NXT-OBS-140` / `TASK-0046`. Repo sınırı yalnız `zbet-mobile/expo-app`;
`zbet-cap` bu batch'te hiç değiştirilmedi.

## Ne yapıldı

### Ortak sözleşme katmanı

`src/api/schemas.ts` üç sağlayıcı-bağımsız sözleşme kazandı:
`team-form.v1`, `match-path.v1` ve `jinx-match-outlook.v1`. Üçü de
`origin: LIVE | SYNTHETIC` taşır ve üçünün de `.catch` düşüşü `SYNTHETIC`'tir.
Yön kasıtlıdır: ayrıştırılamayan köken bilinmeyen kökendir ve bilinmeyen bir
kökenin iddia edebileceği tek şey kanıt olmadığıdır.

`points`/`timeline` gibi listelerde `null` (alınmadı) ile `[]` (alındı, içerik
yok) ayrımı Live Context sözleşmesindeki kuralla birebir aynı tutuldu.

Rotalar `contracts/mobile-api.openapi.yaml` içinde bildirildi ve
`schemas.test.ts` içindeki rota-tamlık kapısı üç yeni yola göre güncellendi.

### Yerleştirme kapısı

Üç yüzey `EXPO_PUBLIC_MOBILE_INTELLIGENCE` bayrağına bağlandı; varsayılan olarak
yalnız preview/mock derlemelerinde açık. Pilot APK bu rotaları sunmayan bir
BFF'e maç başına üç istek atmaz ve sahibe kalıcı olarak `kullanılamıyor`
yazan üç kart göstermez. BFF rotaları yayımlandığında bayrak `true` yapılır;
kod değişmez.

Bundle kanıtı: preview ve pilot profillerinin Android production bundle'ı aynı
hash'i üretti, yani bayrak manifest'te yaşıyor, JS'te değil. Grafik yeniden
yazılmadan önce `index-7b25af933f7279c43d183bef9b6aaf53.hbc` (5.6 MB), sonra
`react-native-svg` ile `index-ae3450d43a076433d9fe42de25d76031.hbc` (5.8 MB).
Her iki ölçümde de iki profil aynı hash'i verdi.

### `NXT-OBS-138` — Takım formu kartı

`src/components/team-form-view.ts` saf karar katmanı, `TeamFormCard.tsx`
sunum. Eksik değer sıfır olarak basılmaz, satır tamamen düşer; küçük örneklem
gerçek sayılarla yazılır ve eşik sözleşmeden okunur; `matchesSampled=0` `EMPTY`,
veri yokluğu `UNAVAILABLE` olarak ayrılır. Çift-bar dili canlı istatistik
modülüyle aynı.

### `NXT-OBS-139` — Maç yolu ve normallik grafiği

Grafiğin son biçimi aşağıdaki tasarım karşılaştırması bölümünde anlatılıyor.
`src/components/match-path-chart.ts` + `MatchPathChart.tsx`. Modülün tek kuralı
`eventSurprise` ile `cohortSize` arasında hiçbir türetme olmamasıdır: kohort her
maçta daralır, dolayısıyla daralma tek başına bilgi taşımaz. Sinyali verilmemiş
nokta işaretsiz çizilir.

Sahibin örneği fixture olarak kuruldu: kickoff 200, devre 100, 60' deplasman
golü sonrası 30, 75' durumunda 24. Devre noktası kohortu yarıya indirirken
sürprizi `0.18`de kalır; deplasman golünde `0.79`a çıkar. Grafik tam olarak bunu
gösteriyor.

Eşiğin altındaki kohort gizlenmez, soluk çizilir. Uyarı metni kohort
daralmasının sürpriz olmadığını açıkça söyler.

### `NXT-OBS-140` — Bilgilendirici `Ask Jinx`

`src/mascot/jinx-match-outlook.ts` + `AskJinxCard.tsx`. Yüzey sorulmadan `IDLE`
kalır ve sorgusu kapalıdır; Maç Detayı açmak hiçbir istek üretmez.

İstemci tarafında ikinci bir dil kapısı var: tavsiye/bahis, kesin gelecek
iddiası ve model/Super/rating sözlüğü reddedilir. Eşleştirme günlük Jinx quip
yolundaki gibi diakritikten bağımsız; ortak `foldDiacritics` iki muhafızın
paylaştığı yeni `src/mascot/jinx-language.ts` dosyasına taşındı (quip
davranışı değişmedi).

Reddedilen başlık yumuşatılmaz, yüzey `UNAVAILABLE` raporlar. Tek istisna
belirsizlik notudur: reddedilirse kaybolmaz, sabit bilgilendirme uyarısına
düşer — kaybolan bir çekince okumayı olduğundan kesin gösterir. Güven yüzde
yerine bant olarak sunulur.

### Modül yerleşimi

`liveDetail` düzenine `matchPath`, `askJinx` ve `teamForm` eklendi. `matchPath`
`gamePulse`'a, `askJinx` `timeline`'a, `teamForm` `askJinx`'e çapalandı.
`timeline` çapası `gamePulse` yerine `matchPath` olarak yeniden hedeflendi:
after-çapasıyla "timeline'dan önce" ancak böyle ifade edilebiliyor ve
`TASK-0045` grafiği Super akışının önüne koyuyor.

## Cihaz doğrulaması

Android 15 `x86_64` emülatörde, mock/preview profilinde, Metro üzerinden.

- `472910` (POPULATED): grafik sürekli normallik çizgisini, 45'te içi boş,
  60'ta dolu violet sürpriz işaretini, 75'te çizginin yeniden yükselişini,
  `200 / 100 / 30 / 24` kohort satırını (24 soluk) ve düşük kohort uyarısını
  gösterdi. Takım formu kartı 7 satırın tamamını ve iki
  takımın W-D-L kaydını gösterdi. `Jinx'e sor` `IDLE` durumundan başlığa,
  gövdeye, üç yönlü sinyale, `Orta güven`e, belirsizlik notuna ve sabit
  bilgilendirme uyarısına geçti.
- `472961` (UNAVAILABLE): üç yüzey de kendi `kullanılamıyor` metnini gösterdi
  ve `Maç detayının geri kalanı etkilenmez.` dedi; Oran sonuçları ve saha
  dengesi normal render edildi.
- Erişilebilirlik: UI hiyerarşisi dökümünde sinyal çipleri
  `Destekliyor: …`, `Karşı: …`, `Nötr: …` olarak okunuyor.
- Açık tema ayrı bir bundle ile (`EXPO_PUBLIC_THEME_MODE=light`) doğrulandı.

## Açık temada bulunan ve düzeltilen kusur

Sürpriz metresi başta `semantic.warning` kullanıyordu. Açık palette gold
`#8A6410`, bronze `#8A5F2B` — yani sürpriz sinyali yanındaki kohort barından
ayırt edilemiyordu. Bu, modülün var oluş nedeninin tam tersi.

Düzeltme ham renge uzanmak yerine anlam katmanına yeni bir rol ekledi:
`semantic.surprise`, palette'in bilinçli olarak boş bırakılmış `violet`
tonuna bağlandı. Her iki temada da bronze ve intelligence mavisinden ayrık.
`SemanticPalette` `violet` alanını kazandı ve `semantic.test.ts` bu ayrımı
doğrulayan bir test aldı.

Ayrıca fixture etiketlerindeki dakika tekrarı (`60' 60' deplasman golü`)
giderildi; dakika yalnız `minute` alanından geliyor.

## Tasarım karşılaştırması ve `NXT-OBS-139` grafiğinin yeniden yazımı

Sahip, maddeyi doğuran Codex tasarım oturumunun ekran görüntülerini paylaşıp
iki tasarımın karşılaştırılmasını ve daha iyi olanın uygulanmasını istedi.

Codex mockup'ı: iki bantlı (`Normal` / `Surprise`) tek bir çizgi, olayda
kesikli dikey işaret, altta ikincil `Similar` sayı satırı, üstte
`SURPRISE EVENT` rozeti ve bir uyarı cümlesi.

İlk uygulamam: kohort büyüklüğü dikey bar, altında iki ince yatay metre
(sürpriz ve normallik), sütun başına etiket.

Karar: **bilgi mimarisinde Codex tasarımı daha iyi, renkte ilk uygulama daha
iyi.** Gerekçeler:

- Süreklilik: `stateNormality` her noktada var olan bir durum,
  `eventSurprise` ise ayrık bir olaya bağlı ve sözleşme bunu zaten
  söylüyor — başlangıç ve düz durum noktalarında `null`. Çizgi + işaret bu
  yapıyı gösterir; iki özdeş metre ikisini aynı türde ölçüm gibi gösteriyordu.
- Toparlanma okunabilirliği: "60'ta sürpriz, sonra normale dönüş" bir çizgide
  tek bakışta görülür, ayrı sütunlarda görünmez. Sahibin Codex notunda özellikle
  vurguladığı davranış buydu.
- Öncelik: ilk uygulamada kohort barları karttaki en büyük öğeydi; oysa kohort
  tek başına sürpriz bilgisi taşımayan boyut. Codex'in yaptığı gibi eksen
  altında ikincil bir sayı satırına indirildi.

Codex'ten alınmayan tek şey renk: mockup sürprizi kırmızı çiziyor, fakat bu
uygulamada kırmızı `semantic.negative`, yani **kaybeden karar** demek. Sürpriz
kayıp değildir. Bu batch'te eklenen `semantic.surprise` (violet) korundu.

Uygulanan yeni yapı:

- `stateNormality` sürekli çizgi; üstte `Olağan`, altta `Sıra dışı`. Ölçülmemiş
  normallik çizgiyi kırar, araya köprü kurmaz — verilmemiş bir durum çizilmez.
- `eventSurprise` çizgi üzerinde işaret: eşik altındaki olay içi boş halka,
  eşik üstündeki dolu violet nokta ve kesikli dikey iz.
- Kohort eksen altında sayı satırı; eşiğin altındaki değer soluk.
- `SÜRPRİZ OLAY` / `OLAĞAN SEYİR` rozeti; hiç sürpriz ölçülmemişse rozet yok —
  `UNMEASURED` sessizce `TYPICAL` sayılmaz.
- Olayı adlandıran uyarı cümlesi; kohortun daralmasını olayın *sonucu* gibi
  sunmaz.

Vurgu eşiği (`notableSurpriseThreshold = 0.6`) sunum eşiğidir, model eşiği
değil: yalnız neyin vurgulanacağını değiştirir, hiçbir değeri gizlemez veya
uydurmaz. Testle sabitlendi.

Hizalama: her düğüm kendi etiket sütununun tam ortasına oturuyor; sütun
genişliği dört noktalı yolun telefonda kaydırmasız sığacağı şekilde seçildi.
Daha uzun yol yatay kayar.

Grafik `react-native-svg` ile çiziliyor. Paket zaten doğrudan bağımlılıktı ve
autolinking'de (`com.horcrux.svg.SvgPackage`) kayıtlı; bu batch'e kadar
uygulamada hiç kullanılmamıştı, bu yüzden emülatörde bilerek doğrulandı.

Doğrulama: `npm run check` **537/537** geçti; grafik Android 15 emülatöründe
koyu ve açık temada render edildi.

## Doğrulama sonucu

```text
npm run check      typecheck + ESLint + 537/537 test + marka kapısı  GEÇTİ
expo export        android production bundle · preview profili       GEÇTİ
expo export        android production bundle · pilot profili         GEÇTİ
                   (grafik yeniden yazımından sonra ikisi de tekrar)
expo-doctor        1 kontrol başarısız                               ÖNCEDEN VAR OLAN
```

Doctor'ın tek başarısız kontrolü önceden bilinen patch-sürüm sapmasıdır
(16 paket, hepsi patch). Bu batch'ten kaynaklanmıyor.

Ortam notu: kabuk ortamında `EXPO_PUBLIC_MOBILE_PILOT_KEY` tanımlı ama
`EXPO_PUBLIC_MOBILE_AUTH_MODE` tanımsız olduğu için `auth-mode.ts:69`
muhafızı config çözümlemesini reddediyor. Bu önceden var olan bir ortam
durumudur, kod değişikliğinden kaynaklanmaz; kapılar açık profil değişkeniyle
çalıştırıldı.

## Kapanış — commit/push, sentetik mod ve fiziksel telefon APK'sı

Sahip doğrulanmış batch için commit/push, deploy ve cihaz APK'sı istedi ve
akışın observation moduna kadar tamamlanmasını söyledi.

### Deploy hedefi yok

`zbet-cap` bu batch'te hiç değişmedi, dolayısıyla rollout edilecek bir BFF
değişikliği ve yeniden başlatılacak bir runtime yok. BTP Work Zone deploy'u
`observation moduna kadar devam et` akışının dışındadır. Bu yüzden bu batch'te
hiçbir deploy adımı çalıştırılmadı; atlanmadı, geçerli bir hedefi yok.

### APK ile bayrak çatışması ve çözümü

Batch'in ilk hâlinde üç yüzey `EXPO_PUBLIC_MOBILE_INTELLIGENCE` bayrağına
bağlıydı ve bayrak yalnız preview derlemelerinde açıktı. `build-pilot-apk.ps1`
ise `EXPO_PUBLIC_USE_MOCKS=false` ve `AUTH_MODE=pilot` değerlerini zorunlu
kılıyor; bu kombinasyonda bayrak `false` çözülür. Yani o hâliyle üretilen pilot
APK üç yüzeyi taşır ama hiçbirini mount etmez — sahip telefonda yeni hiçbir şey
görmezdi. Bayrağı zorla açmak da çözüm değildi: pilot BFF bu üç rotayı
sunmadığı için üç kart kalıcı olarak `kullanılamıyor` gösterir ve her maç
açılışında üç ölü istek atardı.

Sahip seçeneği görüp **sentetik, açıkça rozetli** seçeneği onayladı.

Bayrak üç durumlu bir moda dönüştürüldü: `OFF`, `SYNTHETIC`, `LIVE`. Yüzeylerin
gösterilmesi ile sayıların nereden geldiği ayrı sorulardır; bunları tek bir
boolean'da birleştirmek, bir derlemenin uydurma veriyi ölçülmüş gibi sunmasının
yoludur.

`SYNTHETIC` yalnız üç M15 rotasını yerleşik fixture'lardan karşılar; diğer her
rota gerçek API'yi kullanmaya devam eder. Dürüstlüğü sağlayan şey sözleşmenin
`origin: SYNTHETIC` alanı ve her kartın taşıdığı görünür örnek-veri rozetidir.

Mod derleme zamanında bildirilir, çalışma zamanında asla çıkarsanmaz. Özellikle
"rota 404 verirse fixture'a düş" davranışı **kasıtlı olarak yoktur**: böyle bir
fallback veri kaynağını bir ağ sonucuna bağlardı ve geçici bir kesinti,
ekranda hiçbir şey değişmeden ölçülmüş veriyi uydurma veriyle takas edebilirdi.

Sarmalayıcı önce temel API'yi yayar, sonra tam olarak üç metodu değiştirir;
böylece `MobileApi`'ye sonradan eklenen bir rota unutulma yoluyla sentetik hâle
gelemez. Durumu maç anahtarını hash'leyerek seçer, mock maç kimliğine göre
değil: gerçek fikstür listesinde o kimlikler bulunmadığı için aksi hâlde her
canlı maç aynı duruma düşerdi.

Tanınmayan bir `EXPO_PUBLIC_MOBILE_INTELLIGENCE` değeri artık derlemeyi
durduruyor, sessizce `OFF`'a düşmüyor.

### Commit / push

```text
zbet-mobile  e76bddd  M15 yüzeyleri, sözleşmeler, testler, sözleşme dokümanı
zbet-mobile  1fc47bc  sentetik mod (OFF / SYNTHETIC / LIVE)
btb-codex   0c12834  M15 şerit kaydı, task kayıtları, agent testleri, run index
BTB Logs    3344ebe  BTB_ROADMAP.md M15 şeridi
zbet-cap    c63b729  değişmedi
```

Her repo `origin` ile senkron. `btb doctor` root config sapması bildirmiyor;
`btb status` tüm repoları `dirty=0 in sync` görüyor.

`btb-codex` ve `BTB Logs` değişiklikleri bu batch'e ait değildi; önceki
thread'in M15 şerit kayıtlarıydı ve sahip açıkça onlar da commit edilsin dedi.
Commit öncesi ikisi de gözden geçirildi ve gizli anahtar taraması temiz çıktı.

### Fiziksel telefon APK'sı

Temiz pushed `1fc47bc` kaynağından tek ARM64 pilot artefaktı üretildi.

```text
Path    : C:\dev\btb-cdoex\zbet-mobile\expo-app\.codex-artifacts\btb-mobile-next-arm64-pilot-1fc47bc.apk
Package : com.btb.mobile.next
Version : 0.1.0 · minSdk 24 · targetSdk 36 · compileSdk 36
ABI     : arm64-v8a (yalnız)
Size    : 54.257.117 bytes
SHA-256 : 61B64220B4B3507A2272595FBC98BA01162601697F5C607C4A1A8372E4E470DC
Signing : v2 · fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c
Config  : authMode=pilot · useMocks=false · API=https://api.surklase.com
          mobileIntelligence=SYNTHETIC
Source  : 1fc47bc
Status  : FINAL_PILOT_CANDIDATE · dağıtılmadı · fiziksel Xiaomi kabulü bekliyor
```

İmza sertifikası önceki pilot APK'larla aynı, dolayısıyla telefonda kaldırmadan
üzerine kurulabilir.

Artefakt doğrulaması APK'nın içinden yapıldı:

- gömülü `assets/app.config` `mobileIntelligence: SYNTHETIC`, `useMocks: false`,
  `authMode: pilot` ve gerçek pilot API adresini taşıyor — yani üç yüzey
  telefonda gerçekten mount olacak;
- `lib/arm64-v8a/libreact_codegen_rnsvg.so` mevcut, grafiğin native bağımlılığı
  pakette;
- Hermes bundle'ında M15 arayüz metinleri bulundu. Hermes ASCII olmayan karakter
  içeren dizeleri UTF-16 tablosunda tuttuğu için düz bayt araması Türkçe
  metinleri bulmaz; arama UTF-16LE olarak tekrarlandığında başlıklar, rozet,
  eksen etiketleri, uyarı ve çekince parçalarının tamamı çıktı;
- `private_key`, `client_secret`, service-account, PEM/RSA, `JSESSIONID`,
  legacy FCM server key, `AIza…` ve Gemini host/anahtar kalıplarının hiçbiri
  bulunmadı.

Önceki `btb-mobile-next-arm64-pilot-8be2daf.apk` Geri Dönüşüm Kutusu'na taşındı;
geri alınabilir. `.codex-artifacts` altında yalnız bu APK ve marka kaynakları
kaldı.

APK 51,7 MiB olduğu için sohbet üzerinden telefona/web'e aktarılamadı (30 MiB
sınırı); yalnız bu makinedeki masaüstü uygulamasından erişilebilir.

### Bu kapanışta yapılmayanlar

Deploy yok (geçerli hedef yok), BFF runtime restart yok, SAP/Firebase/Cloudflare
değişikliği yok, production imzalama veya dağıtım yok, Cordova cutover yok.
Gerçek Similar Match hesabı `TASK-0044` / M9, gerçek merkezi Jinx analisti
`TASK-0011` / M11 altında bloklu kalır.

Açık kalan tek kapı fiziksel Xiaomi kabulüdür.

## Batch'in yerel aşaması

Yerel aşama kapandığında commit/push, deploy ve APK henüz yapılmamıştı; sahip
ardından bunları açıkça onayladı ve akış yukarıdaki kapanış bölümüyle
tamamlandı. Model/Super/rating etkisi ve SAP/kalıcılık değişikliği hiçbir
aşamada olmadı; `zbet-cap` dosyaları hiç değişmedi.
