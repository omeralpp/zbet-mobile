# Mobile Next cutover — 2026-08-24 (02)

## Scope and disposition

`mobile cutover start` froze the only actionable `OBSERVED` row,
`NXT-OBS-117`. This was a bounded classification run; no Mobile, BFF, SAP,
Firebase, Cloudflare, runtime, APK, or external-system change was made.

| Item | Final status | Disposition |
| --- | --- | --- |
| NXT-OBS-117 | DEFERRED | The candidate source change is blocked by the active M4 rating freeze and requires post-checkpoint ownership plus live SAP origin evidence. |

## Evidence

- Mobile consumes the current-match hero rating that the BFF maps from
  `zbet_cds_005.Changed`.
- The same response maps the decision minute from `last_super_elapsed`.
- The local CDS snapshot projects both `last_super_elapsed` and
  `last_super_rating` from the same `_LastSuperLog` association.
- Mapping the hero rating from `last_super_rating` is a plausible BFF-only
  candidate, but it changes the displayed Super rating source and therefore is
  not permitted during the active M4 observation freeze.
- The canonical M4 authority forbids model, threshold, rating, and gate changes
  until the forward-evidence checkpoint. The 7-day checkpoint is due at
  `2026-08-27T00:48:51Z`.
- Live SAP origin evidence was not requested because it cannot unlock an
  implementation while this gate remains closed. No live SAP read is claimed.

## Validation

- `zbet-mobile`, `zbet-cap`, and `zbet-abap` were clean before classification.
- No application or contract file changed, so Mobile quality, BFF quality,
  runtime restart, native smoke, and APK stages were correctly skipped.
- Documentation diff and repository status were checked at closure.

## Closure

The task returned to observation mode. Re-evaluate `NXT-OBS-117` after M4's
7-day checkpoint under the BTB owner; verify `last_super_rating` in the live
OData surface before any BFF source change. Commit/push, deployment, SAP write,
release distribution, and APK production remain separate approvals.

`btb next cutover start sonlandı`
