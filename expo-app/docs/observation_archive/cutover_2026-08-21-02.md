# Mobile Next cutover — 2026-08-21 (02)

## Scope

`mobile cutover start` was routed to the canonical `btb next cutover start`
local batch. The frozen input was the eight stale `OBSERVED` rows in
`OBSERVATION_LOG.md`: `NXT-OBS-073`, `074`, `086`, `089`, `095`, `096`, `099`,
and `100`.

The batch authorized reversible local implementation and validation only. It
did not authorize commit/push, deployment, SAP writes or activation, APK
distribution, Firebase/Cloudflare changes, or production work.

## Classification

| Item | Final status | Disposition |
| --- | --- | --- |
| NXT-OBS-073 | DEFERRED | Participant IDs require the SAP DDIC/CDS/OData owner. |
| NXT-OBS-074 | DEFERRED | Producer continuity requires SAP job/SM59 telemetry. |
| NXT-OBS-086 | DEFERRED | Toto idempotency belongs to `BTB Toto - Aktif`. |
| NXT-OBS-089 | DEFERRED | Live verdict columns physically passed; Super profit-band redesign still needs an owner decision. |
| NXT-OBS-095 | RESOLVED | Xiaomi live screenshot and public collector telemetry passed. |
| NXT-OBS-096 | SUPERSEDED | Later controlled evidence replaced the early curl/Node diagnosis. |
| NXT-OBS-099 | RESOLVED | The original SAP bridge activation remains historically complete. |
| NXT-OBS-100 | DEFERRED | A natural red-card event remains observation-only and is not a release gate. |

No frozen item remains `OBSERVED`. The new durability issue is tracked
separately as `NXT-OBS-104` (`READY`).

## Physical and runtime evidence

- The owner-provided Xiaomi screenshots showed Minsk–Vitebsk live at 57′,
  score 0-1, stable Match Detail and Game Pulse, plus the 48′ `Ms2` Super
  decision. The live verdict row displayed `Seçim / Seçim oranı / Güncel oran`
  as intended.
- Public prospective telemetry reported 246 attempted and 246 captured cycles,
  0 failed cycles, 464 matches observed, and a current `lastCycleAt`.
- The SAP bridge SICF route first returned 404. After the owner recreated the
  `detail` node with `ZBET_CL_PROVIDER_BRIDGE`, read-only checks briefly proved
  401 without a token, 200 with a token, and BFF `availability=OK` with three
  timeline events. The closing read-only check returned 404 again.
- SAP ADT MCP repository tools were unavailable. The SAP statements above are
  direct HTTP evidence, not live ADT/MCP source inspection. No SAP mutation was
  performed.

## Local BFF hardening

The dirty local `zbet-cap` checkpoint adds passive
`/health.liveContextTelemetry` visibility. It exposes only safe scalar counters,
last outcome, last HTTP status, and observation time already produced by real
match-context calls. The health endpoint does not probe SAP or create additional
upstream traffic.

Changed local files:

- `srv/live-context/bilyoner/index.js`
- `standalone-server.js`
- `test/live-context-cache.test.js`
- `test/standalone-server.test.js`
- `docs/mobile-live-context.md`

## Validation

- Targeted BFF tests: 34/34 passed.
- Official Mobile BFF quality gate: full tests passed; production build passed.
- Mobile application source was unchanged, so no Mobile rebuild or APK was
  produced.
- No commit, push, deployment, SAP write/activation, or external-system change
  was performed.

## Next controlled gates

1. Under separate SAP approval, export/version the SICF node in the ABAP
   repository, activate it in the correct target, and read it back after a
   restart/session boundary.
2. Under separate commit/push approval, commit the bounded `zbet-cap` telemetry
   patch and Mobile documentation checkpoint.
3. Under separate exact-SHA DEV deploy approval, publish the BFF patch and verify
   `/health.liveContextTelemetry` on the public runtime. Production remains out
   of scope.
