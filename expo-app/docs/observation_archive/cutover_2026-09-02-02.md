# Mobile cutover — 2026-09-02, batch 02

## Later follow-up — 2026-09-02, NXT-OBS-147 local score-scope correction

Owner's physical Motherwell / Dundee Utd. comparison showed that the live BFF
skipped 16 August's Stenhousemuir 1-0 Motherwell result and substituted an older
draw because the secondary Ot scores differed. After read-only diagnosis, owner
said "let's do it". This authorizes the bounded local fix, not commit or rollout.

CAP normalizer now accepts a valid main-score pair across differing secondary
scores only when displayed score and own-team result corroborate it. Malformed,
identity, duplicate and chronology guards remain. ABAP/model and Mobile source
unchanged. Authored synthetic-ID regression failed before the fix; all 51 Team
Form tests and the full BFF tests/production build pass after it. Real SAP match
and statistics through a temporary local HTTP route and the actual Mobile schema
return B M M M G / 1G1B3M / PPG 0.80 / GA 2.20; Dundee and both venue PPG values
are unchanged. Direct bridge HTTP evidence, not MCP repository inspection.

Public pilot still returns old B M M G B / 1.00 / 2.00. Test server closed; no
persistent service/config changes, payload persistence or APK rebuild. After
verification, owner separately approved scoped commit/push: CAP's three source/test
files are pushed as `91cbc25`; four Mobile documents preserve this checkpoint and
earlier APK evidence. NXT-OBS-147 READY; separately approved pilot restart/public
read-back and physical data acceptance remain. Existing APK/hash below is
sufficient. No new cutover run;
Mobile remains OBSERVATION. CURRENT_HANDOFF.md carries the latest checkpoint.

## Later follow-up — 2026-09-02 23:17 TRT, owner-only real Team Form APK

The original local-batch record below is historical. M15 has since closed and
the identity fix is committed (CAP `86bf79e`); the service is enabled. Owner
requested continuation of Claude's enable/build work, then confirmed continuing
with the documented dependency-version caveat for the owner-only pilot.

- Mobile application source `b75b4cd`, unchanged. Canonical ARM64 build passed
  in 5m57s, 1,077 tasks, with process-local two-worker/CPU caps and in-process
  Kotlin to avoid the previous Windows quota failure. No persistent build settings
  or source changes. Stage `C:\btb-mobile-pilot-26988` removed by the builder.
- Candidate: `btb-mobile-next-arm64-team-form-live-b75b4cd.apk`, 54,259,061 bytes.
  SHA-256: `53B366747517A8E5B9211AB85EE72D3ABF8D8173E16324736E59BFA9E2D20D99`.
  `com.btb.mobile.next`, 0.1.0 (1), arm64-v8a only, v2 verification PASS, same
  Android Debug pilot cert `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`.
- APK-internal config: pilot auth, mocks false, api.surklase.com, Team Form LIVE,
  global intelligence SYNTHETIC. Seven bundle UI/contract markers, Jinx artwork
  and Work Zone base pass. 1,321 ZIP entries scanned against seven configured
  server-secret variants/private-key patterns: zero matches. Expected owner-pilot
  client credential is not a production auth mechanism; no values recorded.
- Public BFF plus actual Mobile schema/wrapper pass: Motherwell / Dundee Utd.
  `DLLWD` / `WLLLD`; Dundee / ST Johnstone `LWLWL` / `LLWLW`; Millwall / Wrexham
  `LWWWL` / `LDDLD`. Both sides LIVE/OK with five results consistent with totals.
  Other two cards remain synthetic. Public health/dashboard/Super/matches passed;
  local/public health 200 at 23:17, existing PID 6552 unchanged by Codex.
- Fresh device-smoke limitation: blocked Metro launch; direct new APK installation
  passed on API35 x86 emulator with advertised ARM64 translation, but SoLoader
  looks in lib/x86_64 and fails before JS. Old b9ca7ae ARM64 control reproduced
  the failure. Their ARM64 libreactnative.so hashes are equal:
  `F154A1CEA79CEE52B1523C96CC921115ECBA031FA7CBD722C7F616B610FE494D`.
  Restored the existing x86_64 debug APK, confirmed successful process start,
  stopped the test app and returned the pre-existing emulator home. No uninstall,
  app-data clear or physical Xiaomi test. Do not report a new LIVE UI pass.
- Existing Mobile 562-test and BFF 409-test/build evidence reused. Doctor remains
  19/20 (16 patch mismatches); automatic npm-ci audit 21 findings, 15 moderate and
  6 high, not triaged/fixed. Owner-only pilot caveat, NOT production clearance.
- Verified old APK and temporary batch02/resume evidence were recycled, recoverable.
  Only final phone APK retained; existing brand reference/source folders preserved.
  No stage/commit/push, SAP/model changes, new cutover run or M9/M11 advancement.

Next is the owner's install-over-current and physical check: real Team Form with
no sample badge, correct newest-first G/B/M chips, Jinx heading icon, and sample
badges still on Match Path/Jinx commentary. NXT-OBS-142/145/146 remain READY,
physical acceptance pending. CURRENT_HANDOFF.md carries the active checkpoint.

## Original local-batch record

Status: CLOSED_LOCAL_WITH_DEFERRED_ITEMS — uncommitted checkpoint, no release.
Owner commands: `btb next cutover start`, then `continue`.

## Frozen scope

- NXT-OBS-142: real Team Form BFF connection, with an explicit per-feature
  rollout switch and no synthetic fallback on live failure.
- NXT-OBS-145: ordered last-five result contract and compact team-header strip.
  Local preview uses W green / D yellow / L red with G/B/M labels. Loss colour
  still needs owner clarification before a new APK; `continue` did not specify it.
- NXT-OBS-146: existing Jinx artwork as a static compact module-heading icon.

NXT-OBS-143 (real Match Path) and NXT-OBS-144 (real Jinx analyst) remain deferred
under their existing BTB milestone dependencies. No model or SAP writes.

## Baseline and ownership

- zbet-mobile master f794dda: prior physical-acceptance/observation edits in
  CURRENT_HANDOFF.md and OBSERVATION_LOG.md are preserved.
- zbet-cap main b92c6c7: clean before this batch.
- btb-codex main 6611602: prior three-task acceptance changes in
  state/task-registry.json are preserved; run-index lifecycle only in this batch.
- zbet-abap: read-only source/runtime evidence; no payload changes authorised.

## Validation plan

One final changed-layer pass: BFF route/normalizer/bridge/cache tests and build;
Mobile type/lint/unit/brand/doctor, contract smoke, Android bundle and narrow
screen/dark/light UI smoke. No dependency upgrades. No pilot restart, final APK,
commit/push or release without separate approval.

## Evidence

- SAP MCP source-read operation is not available in this task. Readiness found
  the configured port; this is fallback mode, not live SAP MCP evidence.
- A direct read-only request to the existing bridge for event 3089897 with
  resource=statistics returned HTTP 200 and provider status 200. The actual
  payload exposes homeTeamForms/awayTeamForms with dated team-ID/score rows.
  Raw provider payload was not persisted.

## Results

- **NXT-OBS-145 READY for physical testing:** optional nullable newest-first
  `recentResults` contract, explicit synthetic fixtures, G/B/M header chips,
  ordered accessibility description and no invented chronology from totals.
- **NXT-OBS-146 READY for physical testing:** original static 32 dp Jinx icon
  in the shared optional heading slot. On-demand reading/collapse behavior and
  other module headings retained; no ambient mascot added to Match Detail.
- **NXT-OBS-142 DEFERRED, partial foundation only:** authenticated route,
  bounded DTO cache, fixed statistics bridge method, normalizer and independent
  feature switch implemented. Populated real-data acceptance did **not** pass.
  The actual data exposes incompatible team identity namespaces; see below.
- NXT-OBS-143/144 retain M9/M11 dependencies. NXT-OBS-141 remains READY pending
  the owner's explicit retry confirmation. Prototype TASK-0040/0045/0046 DONE
  statuses from earlier physical acceptance are preserved; they do not imply
  this follow-up is delivered.

## Confirmed real-data blocker

Direct read-only OData and existing SAP bridge HTTP checks, not SAP MCP source
reads, reached real data. Event `3190638` / key
`2026-09-02:3190638:22:00:00` has SAP participant IDs `216` / `349`, while
its Burnley / M'brough statistics groups use team IDs `187` / `273`. Both
groups have 15 history rows. A bounded check of eight current match rows did
not produce a correctly joined populated response (some matches are unavailable
through the existing match route). A prior-day bounded query returned no rows.

An initial naive join incorrectly represented this as zero history; the final
normalizer fails closed on an inconsistent identity join, including an
accidental opponent-ID match. Final real HTTP -> Mobile schema validation
returns `origin=LIVE`, `availability=UNAVAILABLE`, null sides/capture time.
Four populated/empty/missing fixture DTOs also pass the actual Mobile schema.
This proves failure handling, **not** a working real-form rollout.

Next engineering prerequisite is an authoritative statistics-team identity
crosswalk. No names, hard-coded example IDs, synthetic fallback or model tuning
were substituted. The BFF flag stays off; no persistent environment setting
changed. `docs/TEAM_FORM_INTEGRATION.md` records the exact chain and gates.

## Validation evidence

- Mobile `npm run check`: TypeScript, ESLint, **562/562 tests**, brand checks pass.
- BFF official quality adapter: full tests and CAP production build pass after
  the identity guard. Targeted Team Form/bridge suite: **43/43** pass.
- Android production JS export passes (2242 modules, 50 assets); no new native
  module, Gradle APK build, signing or distribution was needed/performed.
- Official Mobile adapter's Expo Doctor phase still fails on the existing
  16-package patch-version drift. No dependencies were upgraded and no warning
  was suppressed. The complete release gate is therefore **not all-green**.
- Emulator API 35, existing compatible debug client, 360 dp width: dark theme
  at 100%/130% text and light at 130% text checked. Five chips, newest-first
  caption, Jinx icon/title and existing collapse controls remain legible.
  On-demand Jinx sample reading works. Sparse/absent/inconsistent sequence and
  unavailable behavior are covered by tests, not claimed as additional phone
  acceptance. No connected physical owner phone was updated.
- Secret review: no known configured SAP/bridge/pilot secret values or private
  key material in the 33 changed source/doc/state files inspected. No raw
  provider payload persisted. Auth, query/key allowlists and bridge credential
  transport tests pass. Git diff whitespace checks pass.
- Existing local and public `/health` remain 200; pilot BFF PID `25412` unchanged.

## Runtime, artifact and handoff

Route remains `https://api.surklase.com`, zbet-cap runtime baseline `b92c6c7`.
Owner phone artifact remains
`.codex-artifacts/btb-mobile-next-arm64-pilot-1fc47bc.apk`, SHA-256
`61B64220B4B3507A2272595FBC98BA01162601697F5C607C4A1A8372E4E470DC`,
still `mobileIntelligence=SYNTHETIC`. This batch is not in that APK.

Changed source groups: Mobile schemas/OpenAPI, feature selection and fixtures,
TeamFormCard/view/tests, Jinx heading slot/icon and Match Detail query mounting;
BFF runtime/provider/router, fixed bridge statistics method, Team Form
normalizer/service/tests and startup flag loader. Mobile handoff/observation/
integration/archive docs updated. btb-codex changes are only preserved task
acceptance plus canonical run-index lifecycle; no shared routing edits.
zbet-abap and BTB Logs remain untouched. No stage/commit/push performed.

Ignored emulator screenshots, local contract-check helper and Android JS export
are under `.codex-artifacts/batch02`; they are temporary validation evidence,
not a phone deliverable. Retain the current verified ARM64 APK. Apply the
procedure's recoverable artifact cleanup only when a new final phone artifact
has been separately approved and verified.

Session cleanup completed: stopped only the Metro server and emulator started
for this batch, removed its 8081 reverse forwarding, and restored emulator
density 420, font scale 1.0, dark theme and normal Jinx tips. Pilot BFF PID
25412 remained untouched. Canonical run writer closed the run (zero open runs);
`btb next cutover start sonlandı` and `btb status` confirm Mobile OBSERVATION,
zero OBSERVED items, no required action. Run-index contents returned to the
baseline; btb-codex retains only the earlier task-acceptance diff. This is a
local batch closure with deferred work, not a release or real-form completion.

Approval gates remaining: resolve identity prerequisite before enabling real
Team Form; address/accept the documented Doctor drift; confirm loss colour and
approve the bounded commit/push; separately approve pilot runtime changes and
final APK/distribution. No SAP/Firebase/Cloudflare/Cordova changes are implied.

## Provider handoff closure — 2026-09-02

Owner subsequently asked to leave Codex and continue with Claude. Current
handoff now includes the exact next read-only investigation, local ownership
and preserved dirty-state summary. No fresh investigation or implementation
was started by the closure request. The later Austria Wien / WSG Tirol photos
were recorded as additional evidence for existing observations, not duplicates
or acceptance of an updated APK.

Rechecked all five scoped repositories: whitespace checks pass; baselines and
recorded-upstream alignment are unchanged. Dirty paths: Mobile 21, CAP 10,
tooling 1; ABAP and BTB Logs clean. No stage/commit/push. Rechecked the existing
ARM64 APK SHA-256: unchanged. Local/public health return 200, PID 25412 remains;
8081/8082 have no listener and no emulator process remains. Tests/builds were
not repeated because no implementation changed after their recorded pass.
The known Doctor failure, mapping blocker and separate release gates remain.
