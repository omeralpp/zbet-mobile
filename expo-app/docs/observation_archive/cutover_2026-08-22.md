# Mobile Next cutover — 2026-08-22

## Scope

`btb next cutover start` froze `NXT-OBS-111` through `NXT-OBS-116`. The batch
authorized reversible local Mobile/BFF implementation and validation only. It
did not authorize commit/push, deployment, SAP/Firebase/Cloudflare changes,
release signing, APK distribution, or Cordova cutover.

## Classification

| Item | Final status | Disposition |
| --- | --- | --- |
| NXT-OBS-111 | RESOLVED | Main tab scenes now slide together across the full viewport without fade; the tab bar stays anchored and detail stacks use the same directional motion. Android 15 mid-transition evidence passed and the owner accepted the shared visual evidence. |
| NXT-OBS-112 | RESOLVED | Super decisions are merged into the goal/red-card chronology as neutral star checkpoints and the retired separate module is reconciled out. Unit and populated-emulator timeline evidence passed and the owner accepted the shared visual evidence. |
| NXT-OBS-113 | RESOLVED | The mutually exclusive Open tab became an independent, local, accessible switch that composes with day/star filters. Android 15 filtering `3 -> 1` passed and the owner accepted the shared visual evidence. |
| NXT-OBS-114 | RESOLVED | `Tümü` became `Fikstür`, restricted to `NOT_STARTED`; legacy ALL links normalize to FIXTURE. Unit and Android 15 fixture evidence passed and the owner accepted the shared visual evidence. |
| NXT-OBS-115 | RESOLVED | The main Super history and match-key enrichment limits are 200, the OpenAPI list bound is 200, and exact-match history remains 50. A real 200-row provider fixture preserved order/map and a bounded URL; the owner accepted this technical evidence without claiming a physical 200-row scroll. |
| NXT-OBS-116 | RESOLVED | The separate Jinx halo was removed and the transparent image/anchor grew from 58dp to 68dp without changing stored position semantics. Android 15 dark/safe-area evidence passed and the owner accepted the shared visual evidence. |

No frozen item remains `OBSERVED` or `READY`. On 2026-08-22 the owner accepted
the shared Android 15 visual evidence and the 200-row technical contract/test
evidence, so all six items are retained as `RESOLVED` in the active log.

## Local implementation

- Replaced the small fade/shift tab preset with a shared 280ms full-width,
  direction-aware paired scene transition. Adjacent tabs are preloaded and
  retained, the navigator background stays dark, and reduced motion disables
  travel. Detail routes use directional stack motion.
- Removed the gesture/stack double-exit path on committed main-tab and edge-back
  navigation.
- Added a typed presentation-only match feed that chronologically interleaves
  provider goal/red-card events and historical Super decisions without
  inventing side, score, causality, or second-level ordering.
- Retired `relatedSuper` from the configurable module registry and migration.
- Added the independent Open switch and the `LIVE <-> FIXTURE` local tab model.
- Removed Jinx's separate halo view/animation and enlarged the common visual,
  drag, clamp, menu, and bubble anchor size together.
- Raised only `/v1/super/logs` and its current-match key enrichment to 200;
  `/v1/btb/matches/:key/super-logs` remains 50.

No Super/Toto model, SAP, Firebase, Cloudflare, notification, or unrelated CAP
telemetry behavior changed.

## Validation

- Mobile `npm run check`: TypeScript, ESLint, 414 unit tests, OpenAPI parsing,
  and brand validation passed.
- Mobile BFF official gate: full tests and production build passed.
- Provider contract test: 200 rows, `created_at desc`, `$top=200`, lossless
  mapping, bounded match-key URL; exact-match `$top=50` remained covered.
- Preview Expo Android prebuild passed.
- Android 15 x86_64 debug compile/install/launch passed. The local debug APK was
  `95,954,229` bytes, x86_64 only, SHA-256
  `C6EF48FF94F9AB9D757579BCB77A0BD23C51D7ACE1E6B5108EEDFDB4A21130E2`.
- Emulator smoke passed for paired mid-transition scenes with anchored bottom
  bar, Fikstür-only upcoming content, Open switch filtering, merged timeline,
  and halo-free enlarged Jinx. No fatal Android/React Native log was found.
- Expo Doctor's non-version checks passed, but the current registry check exits
  non-zero for nine pre-existing Expo SDK 57 patch recommendations. Per cutover
  policy no unrelated dependency upgrade was performed.
- Physical Xiaomi parity and a physical 200-row long-list scroll were not run
  because no physical device was attached. The owner accepted the available
  emulator/technical evidence for observation closure.
- After observation closure, the owner requested the missing phone artifact.
  A single ARM64 pilot release APK was produced at
  `.codex-artifacts/btb-mobile-next-arm64-cutover-04.apk`: `53,347,881` bytes,
  package `com.btb.mobile.next` version `0.1.0`, only `arm64-v8a`, APK Signature
  Scheme v2, pilot/debug certificate SHA-256
  `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`, APK
  SHA-256
  `5D0ECB8C19514BCCF75EF8CDC574F7F7760EA3018896A989CEE2355265B77217`.
  Server/private-key/session pattern scanning returned zero hits. Previous APKs
  and temporary screenshots were moved to the Recycle Bin after verification,
  leaving this as the sole retained artifact.

## External and Git gates

The Mobile batch was committed and pushed as `7585aaa`; the Super 200 BFF
contract as `0832b16`; and the prior validated passive Live Context telemetry
checkpoint as `19aa8f8`. The clean pushed CAP `19aa8f8` source restarted the
existing local pilot runtime once. Local and public health exposed
`liveContextTelemetry`, and an authenticated public `/v1/super/logs` read-back
returned exactly 200 rows. No Cloudflare configuration, BTP Work Zone, Firebase,
SAP, production signing, or Cordova mutation was performed. The locally
debug-signed pilot APK was made available in response to the owner's explicit
request. The pre-existing untracked `cutover_2026-08-21-02.md` file was
preserved.
