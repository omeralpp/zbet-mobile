# Mobile Next cutover — 2026-08-22 (02)

## Kapsam

`mobile cutover start` (= `btb next cutover start`) `NXT-OBS-117`–`120`
maddelerini dondurdu. Batch yalnız tersinir yerel Mobile kod/test
değişikliğini yetkilendirdi. Commit/push, deploy, SAP/Firebase/Cloudflare
değişikliği, release imzalama, APK dağıtımı veya Cordova cutover
yetkilendirilmedi — commit/push adımı ayrıca ve açıkça onaylandı (aşağıya
bakınız), diğerlerinden hiçbiri yapılmadı.

## Sınıflandırma

| Madde | Nihai durum | Karar |
| --- | --- | --- |
| NXT-OBS-117 | OBSERVED (blocker) | Kök neden Mobile'ın eşleme mantığında değil, `current-match`/`SuperLog` kaynaklarının atomik olmayan anlık görüntüsünde. Gerçek düzeltim BFF (`zbet-cap`) ve muhtemelen SAP (`zbet-abap`) tarafında source-of-truth/snapshot tutarlılığı gerektiriyor; tek bir Mobile-local dosya değişikliğiyle kapatılamayacak kadar geniş ve belirsiz. Ayrı scoping ve (SAP tarafı etkileniyorsa) ayrı onay gerekiyor. Uygulanmadı. |
| NXT-OBS-118 | RESOLVED (Mobile UI yarısı) | `MatchTimelineCard` artık skor ilerlediği halde timeline gerçekten boşsa "Henüz gol yok" yerine "Skor ilerledi ama olay detayı henüz gelmedi." gösteriyor. Provider/SAP tarafındaki gerçek event-kapsamı gecikmesi araştırılmadı — bu ayrı ve daha büyük bir konu, Mobile-local düzeltmenin kapsamı dışında. |
| NXT-OBS-119 | RESOLVED | `FIXTURE` sekmesi artık `NOT_STARTED` bir maçı yalnız kickoff'tan (Europe/Istanbul, `+03:00` sabit offset) beri geçen süre 90 dakikanın altındaysa gösteriyor; kickoff ayrıştırılamıyorsa eski davranış korunuyor. Erteleme/iptal sonucu uydurulmadı — toleransı aşan maç yalnız listeden düşüyor. |
| NXT-OBS-120 | OBSERVED (blocker) | Kök neden doğru teşhis edildi (`Screen.tsx`'in ana sekme sürüklemesi eski `PanResponder`/JS-thread API'siyle yapılıyor), ama düzeltim `react-native-gesture-handler`/`react-native-reanimated`'ı gerçek anlamda kurmayı (henüz yok olan `babel.config.js`, worklet plugin, `GestureHandlerRootView`) ve uygulama genelinde paylaşılan bir gesture kökünü değiştirmeyi gerektiriyor. Prosedürün "geri döndürmesi zor işlem varsayımla uygulanmaz" kuralı gereği bu batch'e alınmadı; ayrı onay ve fiziksel cihaz doğrulaması gerekiyor. |

Dondurulan dört maddeden ikisi (`118` Mobile yarısı, `119`) `RESOLVED`; ikisi
(`117`, `120`) kapsam/karar belirsizliği nedeniyle blocker olarak `OBSERVED`
kaldı ve aktif günlükte kalmaya devam ediyor — bu ikisi bu arşive taşınmadı.

## Yerel uygulama

- `src/utils/live-match-tabs.ts`: `FIXTURE_STALE_TOLERANCE_MS` (90 dk) ve
  `kickoffInstant()` eklendi; `matchLiveTab`'ın `FIXTURE` dalı artık kickoff'a
  göre süzüyor. `matchLiveTab` yeni opsiyonel `now` parametresi aldı
  (varsayılan `Date.now()`), mevcut çağıranlar değişmeden çalışmaya devam
  ediyor.
- `src/components/MatchTimelineCard.tsx` + `app/match/[key].tsx`: yeni
  `homeScore`/`awayScore` prop'ları eklendi; `state === "EMPTY"` dalında skor
  toplamı > 0 ise iki boş-durum metni de (`stateBody` ve `sourceNoticeText`)
  farklı, dürüst bir mesaja dönüyor.
- `src/utils/live-match-tabs.test.ts`: mevcut test yeni `now` parametresiyle
  güncellendi; toleransın hem içinde hem dışında kaldığı iki durumu doğrulayan
  yeni test eklendi (`NXT-OBS-119`).

Super/Toto model, SAP, Firebase, Cloudflare, bildirim veya ilgisiz CAP
telemetri davranışına dokunulmadı. `NXT-OBS-117` ve `NXT-OBS-120` için hiçbir
kod değişikliği yapılmadı — yalnız değerlendirilip blocker olarak kaydedildi.

## Doğrulama

- Mobile `npm run check`: TypeScript, ESLint, **415/415** test, marka
  doğrulayıcı temiz geçti (exit 0).
- Fiziksel veya emülatör cihaz doğrulaması yapılmadı — bu batch yalnız süzme
  mantığı ve metin/koşul değişikliği içeriyor, yeni APK üretilmedi.
- BFF (`zbet-cap`) bu batch'te değiştirilmedi, ayrıca test edilmedi.

## Dış ve Git kapıları

Sahip commit/push adımını sohbette açıkça onayladı ("onay veriyorum"). Beş
değişen dosya (`app/match/[key].tsx`, `docs/OBSERVATION_LOG.md`,
`src/components/MatchTimelineCard.tsx`, `src/utils/live-match-tabs.test.ts`,
`src/utils/live-match-tabs.ts`) tek tek adlarıyla stage edilip commit
`1fc45ca` ile `origin/master`'a push edildi (`d603e1a..1fc45ca`). Önceden var
olan, bu batch'e ait olmayan untracked `cutover_2026-08-21-02.md` dosyasına
dokunulmadı. Deploy, SAP/Firebase/Cloudflare değişikliği, release imzalama,
APK dağıtımı veya Cordova cutover yapılmadı.
