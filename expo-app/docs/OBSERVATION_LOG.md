# BTB Mobile Next — Observation Log

Durum: `BTB ROADMAP MILESTONE 2/11 — LOGO / BRAND ASSET · KAPALI, FİZİKSEL
OLARAK DOĞRULANDI` · Roadmap **2/11 = %18,2**

Roadmap: **1/11 KAPALI = %9,1**. Milestone 1 (Design V2 fiziksel geri bildirim
kapanışı) sahibin Xiaomi cihazında fiziksel olarak doğrulandı ve kapandı;
çelişkili runtime kanıtı çıkmadıkça yeniden açılmaz. Product Design V2'nin
kendisi dokuz batch ile tamamlanmıştı (`ff50051..21a1f57`) ve yalnız sunumdur;
tahmin, model, Toto, Live Context sözleşmesi, BFF ve SAP davranışı değişmedi.

Milestone 1'den devreden tek erteleme — canlı kart `güncel baskı farkı`
etiketinin Xiaomi genişliğinde uzun kalması — Milestone 2'de kapatıldı
(`f3d628d`). Ayrıntı ve görsel doğrulama `docs/CURRENT_HANDOFF.md` içindedir.

Fiziksel Xiaomi ile doğrulanmış APK (Milestone 1 kapanışı):

```text
btb-mobile-next-arm64-design-v2-feedback.apk
SHA-256: D7B5B5364D8C9E5A9CF8AC44E76147D426D958CCBFF4021B12FADB877CFF684A
```

Marka varlığı 2026-08-19'da entegre edildi (`a8ba66b`) ve yeni ARM64 pilot APK
bu kaynaktan derlendi:

```text
btb-mobile-next-arm64-brand-asset.apk
SHA-256: 522F904E9EB047E072F0C25F98210B98F343DBBD8ED2848D6214E7D50E9A721D
imza   : fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c
```

APK denetimi: launcher foreground 432x432 **%79,2 saydam**, köşe alfası **0**;
sır taraması temiz. Release runtime smoke emülatörde (x86_64 paketi, doğrulama
sonrası silindi) geçti: pilot kapısı, canlı API, gerçek canlı maç, koyu/açık
tema, launcher ikonu ve açılış ekranı. Milestone 2 **2026-08-19'da kapandı**: sahip Xiaomi
fiziksel doğrulamasını tamamladı ve marka entegrasyonunu kabul etti.

Fiziksel Xiaomi ile doğrulanmış geri dönüş baseline'ı (değişmedi):

```text
btb-mobile-next-arm64-live-context-v2.apk
SHA-256: 3096C0335361F45B6B95B7AFD35AE1A1D9C91E4D6233735F998E1B2B34EB5B28
```

Üç APK de aynı imza parmak izini taşır
(`fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`), bu yüzden
aday APK mevcut kurulumun üzerine kaldırmadan kurulur ve panel göçü gerçekten
sınanır. Aday doğrulanana kadar hiçbir APK geri dönüşüme alınmaz.

`LIVE_CONTEXT_V2_ACCEPTED` + `REAL_GOAL_PHYSICAL_PASS` +
`RED_CARD_OWNER_ACCEPTED` + `NO_NEW_APK_REQUIRED` +
`MOBILE_NEXT_BASELINE_VERIFIED` — 2026-08-18 release kapanışı.
Kapanış kanıtı: `docs/observation_archive/cutover_2026-08-18.md`
(UI baseline: `cutover_2026-08-17-03.md`).

Önceki baseline `btb-mobile-next-arm64-pilot.apk` ve doğrulanmamış ara artifact
`btb-mobile-next-arm64-ui-polish.apk` cutover prosedürü adım 8 uyarınca Geri
Dönüşüm Kutusu'na taşındı; `.codex-artifacts` yalnız yukarıdaki doğrulanmış
`arm64` APK'yı tutar.

`BILYONER_LIVE_CONTEXT_RUNTIME = ENABLED_VIA_SAP_BRIDGE` (2026-08-18) —
sağlayıcıya SAP'ın halihazırda çalışan HTTP istemcisi üzerinden erişilir
(`BTB_LIVE_CONTEXT_UPSTREAM=SAP_BRIDGE`). Doğrudan Node yolu kapalıdır ve
kapalı kalır; TLS parmak izi taklidi, çerez/oturum/giriş otomasyonu yapılmadı.

Bu dosya yalnız gerçek kullanımda açık kalan tespitleri tutar. Observation
sırasında kod değiştirilmez. Yeni değişiklik batch’i yalnız kullanıcı
`btb next cutover start` dediğinde başlar.

| ID | Tarih | Alan | Tespit / beklenen kanıt | Öncelik | Durum |
| --- | --- | --- | --- | --- | --- |
| NXT-OBS-001 | 2026-07-29 | Performans widget | KPI parser ve dashboard fallback düzeltildi. Android 15 emülatöründeki gerçek widget Toto kapsamını ve cihazda seçilen kalıcı `1+ / 2+ / 3+ / 4+` Super eşiğinin günlük profit/kazandı/kaybetti değerini doğru gösterdi. Final arm64 APK’nın fiziksel cihazda gerçek bildirim/uygulama dönüşü sonrasında aynı parity’yi koruduğu doğrulanmalı. | HIGH | READY |
| NXT-OBS-002 | 2026-07-29 | Notification görünümü | Android notification küçük ikonu ve varsayılan Firebase/Expo ikon metadata’sı APK’da mevcut. Gerçek FCM bildiriminin fiziksel cihazdaki küçük ikon görünümü bekleniyor. | MEDIUM | READY |
| NXT-OBS-032 | 2026-07-29 | Canlı veri tazeliği | `zbet_t_matches` / canlı ana OData entity’si gerçek kaynak güncelleme zamanı yayınlamıyor. BFF yanıt zamanı SAP veri tazeliği gibi gösterilmedi. Doğru `Son veri ... önce` ve stale uyarısı için kaynakta tutulan timestamp alanı, DDIC/CDS/service aktivasyonu ve ayrı SAP onayı gerekir. | HIGH | DEFERRED |
| NXT-OBS-033 | 2026-07-29 | Windows başlangıç dayanıklılığı | Kullanıcı onayıyla `BTB Mobile BFF` logon task’ı `pilot` profili ve aktif notification teslimiyle yeniden kaydedildi. Task `Ready`, son sonuç `0`; tek 4004 listener ile local/public health iki servis için `configured` ve gerçek FCM teslimi `2/2` geçti. Task argümanında secret yoktur. Gerçek Windows reboot sonrasında otomatik kalkış observation’ı gerekir. | HIGH | READY |
| NXT-OBS-053 | 2026-08-01 | APK dağıtım netliği | Güncel final yalnız `arm64-v8a` v12 APK’dır; v11 doğrulama sonrasında Geri Dönüşüm Kutusu’na taşındı. v12 package/ABI/v2 imza/bundle ve ham Firebase server-secret yokluğu doğrulandı. Fiziksel Android telefonda kurulum, Bibi rehberi, lig/final skor/oran-baskı görünümü, Fiori SAP ID login ve gerçek notification teslimi observation’da doğrulanmalı. | MEDIUM | READY |
| NXT-OBS-058 | 2026-08-09 | Lig sıralaması | Ortak kompakt `Puan Durumu` bileşeni sıra rozetine göre sıralanan `Sıra / Takım / P` görünümüyle Super ve Canlı Maç detayına eklendi. Super ekranı karar anı kaydını, maç ekranı yalnız son Super kararının lig snapshot’ını açık etiketle gösterir. Canlı SAP metadata’sında tam lig `O / G / B / M / A / Y / AV` alanları yayınlanmadığı için veri uydurulmadı; yalnız doğrulanan iki takım sıra/puanı kullanıldı. Mobile 91/91, BFF 63/63 ve izole canlı SAP route smoke geçti. Yeni BFF kaynağı kontrollü restart ile aktif 4004 sürecine yüklendi; fiziksel APK üzerindeki authenticated görünüm observation’ı beklenir. | HIGH | READY |
| NXT-OBS-067 | 2026-08-09 | Yeniden başlatma dayanıklılığı | Reboot sonrası 502 kök nedenleri için startup zinciri açık auth/pilot profilini taşır. Aktif BFF notification teslimi açık biçimde kaldırıldı ve aynı komut Windows logon task’ına kaydedildi. Task son sonucu `0`; local/public health iki servisi de `configured` gösteriyor ve gerçek FCM `2/2` geçti. Gerçek reboot otomatik başlangıç kanıtı beklenir. | HIGH | READY |
| NXT-OBS-068 | 2026-08-09 | Uygulama kullanıcı girişi | v12 pilot cold-start’ında özel BTB giriş sayfası gösterilir; metin kullanıcı adı/parola istemediğini ve gerçek kullanıcı kimliği doğrulamadığını açıklar, buton salt okunur pilot erişimine geçirir. Mobile 91/91 test ve release build geçti; fiziksel kurulum kanıtı beklenir. Gerçek kurumsal kullanıcı login’i hâlâ canlı HTTPS OIDC provider/public client, BFF `oauth` runtime ve Android App Link dış cutover’ını gerektirir. | HIGH | DEFERRED |
| NXT-OBS-070 | 2026-08-09 | Bibi tutorial modu | Sürüm kontrollü Bibi rehberi ilk kurulumda açık gelir; ekran bazlı hedeflere safe-area içinde hareket eder, aynı sayfadaki adımları `Sonraki` ile ilerletir ve kısa konuşma balonu gösterir. İlerleme AsyncStorage’da tutulur; `Rehberi kapat`, `Daha fazla` ekranından aç/kapat ve baştan başlat seçenekleri eklendi. Küçük ekran balon sınırı, overlay dokunma alanı, erişilebilirlik duyurusu ve reduced-motion fallback’i uygulandı. Pure state testleri ve Android release bundle geçti; fiziksel cihaz hareket/dokunma observation’ı beklenir. | MEDIUM | READY |
| NXT-OBS-071 | 2026-08-09 | Super karar sonucu | Super Kararı Detayı karar anı skorunu ayrı etiketler; yalnız `WON/LOST` ve güvenilir `finalScore` doluyken sonuç rengiyle `Biten skor` gösterir. `OPEN/VOID` veya eksik final skorda alan gizlidir. Canlı Super V4 metadata’sında `final_home_s/final_away_s`, mapper’da tarihsel `finalScore` zinciri doğrulandı; current match satırına bağlı değildir. Mobile 91/91 ve BFF 63/63 geçti; fiziksel görünüm observation’ı beklenir. | HIGH | READY |
| NXT-OBS-072 | 2026-08-09 | Canlı kart ikon semantiği | Trend ikonu canlı oran bölümüne taşındı: current seçim oranından yüksekse yukarı, düşükse aşağı, eşitse nötr; oran eksik/kapalıysa yön ikonu hiç gösterilmez. Baskı tarafı ev sahibinde ev, deplasmanda uçak, veri yok/dengede nötr teraziyle gösterilir. Oran ve baskı semantiği ayrı erişilebilirlik metinleri ve pure policy testleriyle doğrulandı; fiziksel küçük ekran observation’ı beklenir. | MEDIUM | READY |
| NXT-OBS-073 | 2026-08-09 | Bilyoner takım logoları / görsel kimlik | Mobile resolver/fallback hazır; fakat güvenilir `htpi/atpi` participant ID zinciri SAP DDIC/CDS/OData ve BFF sözleşmesinde yok. Takım adından tahmin, runtime scraping ve component içi CDN hard-code yapılmaz. İlgili operasyonel task gerçek alanları ayrı SAP yazma/aktivasyon onayıyla yayınlayana kadar Mobile yerel değişikliği yoktur. | HIGH | DEFERRED |
| NXT-OBS-074 | 2026-08-09 | Notification producer sürekliliği | Mobile/BFF health, registry ve Firebase teslimi sağlıklıydı; boşluk producer endpoint’inden önce SAP notification kuralı, job çağrısı veya SM59 sonucundadır. Kesin alt neden SAP tarafında kalıcı, secretsiz telemetry ve ayrı operasyonel onay gerektirir; Mobile cutover işi değildir. | HIGH | DEFERRED |
| NXT-OBS-082 | 2026-08-12 | Windows başlangıç dayanıklılığı | Bilgisayar/oturum dönüşü sonrasında Android uygulaması bağlantı hatası verdi. Yerel `127.0.0.1:4004` listener kapalı ve public tunnel health `502` idi. Windows görevinin argümanı doğru biçimde `-AuthMode pilot` taşımasına rağmen önceki çalışması `0xC000013A` ile kesilmişti; kullanıcının düz `start-mobile-bff.ps1` komutu ise açık profil vermediği için tasarım gereği `BTB_MOBILE_AUTH_MODE` hatası verdi. Mevcut `BTB Mobile BFF` görevi yeniden tetiklendi; local/public health ve authenticated `/v1/dashboard` HTTP 200, görev `Ready / last result 0`, listener PID 25464 doğrulandı. Commit veya Cloudflare/BTP değişikliği yapılmadı. Bir sonraki gerçek Windows reboot/logon sonrasında otomatik ayağa kalkış tekrar gözlenmeli. | HIGH | READY |

| NXT-OBS-083 | 2026-08-12 | Karar Günlüğü gün kapsamı filtresi | Ayrı ve uzakta duran `Bugün / Tüm günler` geçişi yeterince anlaşılır değil. Tek bir kompakt açılır `Gün kapsamı` filtresi kullanılmalı: kapalı durumda takvim simgesiyle aktif seçim (`Bugün` veya `Tüm günler`) ve aşağı ok görünmeli; dokununca tetikleyicinin altında yalnız bu iki seçenek açılmalı. Seçim listeyi ve tarih/kayıt özetini birlikte güncellemeli; `Tüm günler` mevcut sınırlı geçmiş kapsamını (ör. en yeni 100 karar) tarih satırıyla açıklamaya devam etmeli. Dokunma hedefi en az 44–48 dp olmalı, menü ekran dışına taşmamalı, yıldız menüsüyle aynı anda açık kalmamalı ve dışarı dokunma/Android geri/yeniden dokunma ile kapanmalıdır. | MEDIUM | READY |

| NXT-OBS-084 | 2026-08-12 | Detay ekranı özet kartı ve bağlamsal toolbar | Super Kararı Detayı ve canlı maç gibi maç tabanlı detay ekranlarının ilk özet kartında bilgi hiyerarşisi dağınık: lig/tarih/durum, takım eşleşmesi, karar anı skoru, ilk yarı/biten skor ve seçim-oran-kâr bilgileri standart semantik gruplara ayrılmalı; karar anı ile nihai sonuç görsel olarak karıştırılmamalı, hizalar dar ekran ve büyük yazı boyutunda bozulmamalıdır. Kart görünüm alanından çıkınca üst araç çubuğu yumuşak bir geçişle genel ekran başlığından tek satırlık maç adına (`Ev sahibi – Deplasman`) dönüşmeli; böylece kullanıcı sayfanın altındayken hangi maçı incelediğini görmelidir. Geri düğmesi ve Bibi sabit kalmalı, uzun adlar ellipsis ile erişilebilir tam metin taşımalı, başlık kart yeniden görünür olduğunda eski haline dönmeli, sticky toolbar safe-area/Android geri/yatay kaydırma davranışlarını bozmamalı ve açık-koyu temada aynı hiyerarşiyi korumalıdır. | HIGH | READY |

| NXT-OBS-085 | 2026-08-12 | Baskı dengesi doğru detay bağlamı | Canlı Maç Detayı içindeki `Canlı saha dengesi` kartının altındaki ev/deplasman baskısı özet çubuğu bu ekranın güncel istatistik akışını gereksiz uzatıyor ve Super karar bağlamına daha uygun. Çubuk Canlı Maç Detayı’ndan kaldırılmalı; ham canlı metrikler (topla oynama, şut, isabetli şut, xG, korner ve kartlar) yerinde kalmalıdır. Baskı görselleştirmesi Super Kararı Detayı’nda `Karar anındaki saha` bölümüne `Karar anı baskı dengesi` adıyla taşınmalı ve yalnız tarihsel Super Log snapshot’ındaki ev/deplasman baskı değerlerinden üretilmelidir; güncel maç değeri tarihsel karara karıştırılmamalı, veri eksikse sıfır uydurmak yerine `Veri yok` gösterilmeli veya bileşen gizlenmelidir. Ev/deplasman renkleri ve takım yönleri ekran genelindeki mavi/yeşil semantiğiyle aynı kalmalı, değer ve fark erişilebilir metinle açıklanmalıdır. | MEDIUM | READY |

| NXT-OBS-086 | 2026-08-12 | Toto notification idempotency çakışması | Kök neden Toto producer kimliğindedir: aynı gün aynı başlık/gövde stabil Toto olay kimliği taşımadığı için duplicate anahtarına düşebilir. Düzeltme ve SAP aktivasyonu yalnız `BTB Toto - Aktif` sahipliğindedir; Mobile bu iş kararını ilerletmez. | HIGH | DEFERRED |

| NXT-OBS-087 | 2026-08-13 | Kullanıcıya özel Super notification yıldız eşiği | Backend notification tablosundaki minimum yıldız sistem geneli alt sınır olarak kalmalı (ör. `1`); kullanıcı Mobile `Daha Fazla > Bildirimler` altında ayrı bir `Super bildirim eşiği` seçebilmeli: `1+`, `2+`, `3+`, `4+`, `5 yıldız`. Bu tercih liste/Super filtrelerinden bağımsız, yerel ve kalıcı olmalı; varsayılan `1+` mevcut davranışı korumalıdır. Etkin görünür eşik fiilen `max(backend global minimum, kullanıcı tercihi)` olur; Mobile backend’in izin vermediği daha düşük yıldızı geri açamaz. Mevcut Android FCM mesajları data-only çalışıyor ve background task önce widget’ları güncelleyip sonra yerel görünür notification üretiyor. Bu nedenle Android pilot için en güvenli çözüm tüm geçerli veriyi almaya devam edip widget güncellemesini her mesajda çalıştırmak, yalnız `presentAndroidNotification` adımını AsyncStorage’daki kullanıcı eşiğine göre bastırmaktır; böylece düşük yıldız bildirimi görünmez ama performans/widget güncelliği bozulmaz. Seçim UI’si mevcut yıldız seçeneklerini yeniden kullanmalı, değer değişince sonraki mesajdan itibaren etkili olmalı; bozuk/eksik tercih fail-safe `1+` olmalı. General ve Toto bildirimleri etkilenmemeli. Testler eşik altı Super’de `widget updated + notification not presented`, eşik ve üstünde ikisinin de çalışması, tercih kalıcılığı, geçersiz rating ve cold/background task durumunu kapsamalıdır. Pilot Android’ın legacy `BTB` topic aboneliği görünür filtreyi bypass edebilecek ikinci bir üretici hattı olmadığı kanıtlanmalı veya kontrollü biçimde kaldırılmalıdır. OAuth/iOS çoklu cihaz aşamasında hesap bazlı server-side tercih ayrıca tasarlanabilir; mevcut pilotta tercih cihaz/kurulum bazlıdır. | MEDIUM | READY |
| NXT-OBS-088 | 2026-08-13 | Dark/Light switch ve Android widget tema eşleşmesi | `Daha Fazla > Uygulama görünümü` mevcut açılır/uyarı seçimi yerine erişilebilir bir `Switch` olmalı; kapalı durum `Açık`, açık durum `Koyu` temayı temsil etmeli ve görünen metin yalnız renge bağlı kalmadan aktif modu belirtmelidir. Switch en az 44–48 dp dokunma alanı, erişilebilirlik rolü/durum açıklaması ve tema değişirken tekrarlı dokunmayı önleyen kısa geçiş durumu taşımalıdır. Tema tercihi mevcut kalıcı uygulama ayarında tek kaynak olarak korunmalı; seçim değiştiğinde uygulama temasından önce native widget katmanına senkronize edilerek hem BTB bildirim widget’ı hem BTB Performans widget’ı veri kaybetmeden hemen yeniden çizilmelidir. Bilyoner/widget içerik verileri değişmemeli; yalnız arka plan, yüzey, metin, ayırıcı, pasif halka ve vurgu renkleri merkezi light/dark widget paletinden gelmelidir. Native `btb-widget` modülü tek bir `setTheme`/resolver giriş noktası ve ortak preference kullanmalı; CDN veya renk değerleri provider’lara dağınık hard-code edilmemelidir. Uygulama açılışında ve widget yeniden oluşturulduğunda kayıtlı tercih tekrar senkronize edilmeli; eksik/geçersiz değer mevcut koyu tema davranışına güvenli biçimde dönmelidir. İki widget provider’ı aynı preference değişikliğinde `updateAllWidgets` ile güncellenmeli. Testler switch kalıcılığı, uygulama yeniden yüklenmesi öncesi native senkronizasyon, iki modda iki widget’ın okunabilirliği/kontrastı, mevcut widget verisinin korunması, bozuk tercih fallback’i ve fiziksel Android launcher smoke kontrolünü kapsamalıdır. | MEDIUM | READY |

| NXT-OBS-089 | 2026-08-13 | Maç ve Super detay özet kartı görsel parity | Canlı kartın `Seçim / Seçim oranı / Güncel oran` üç kolonu `3396d4b` ile uygulandı ve 2026-08-21 fiziksel Xiaomi görüntüsünde 57′ canlı maçta doğrulandı. Super kartında kârın karar satırından ayrı sonuç bandında tutulması kasıtlı semantiktir; bunu geri almak açık owner tasarım kararı gerektirir ve varsayımla uygulanmaz. | MEDIUM | DEFERRED |

| NXT-OBS-090 | 2026-08-13 | Toto program güncelliği ve ikramiye görünümü | Toto Programı detayında güvenilir `updatedAt` alanı Mobile DTO ve BFF mapper'da mevcut; ekran program başlığı veya Sonuçlar kartı yakınında yerel tarih-saatle `Son güncelleme: 13 Ağu 2026 · 10:02` biçiminde göstermeli ve bu değeri cihazın sorgu zamanı gibi sunmamalıdır. Mevcut Mobile/BFF Toto sözleşmesinde ikramiye alanı bulunmuyor. Sonuç kaynağında doğrulanmış ikramiye mevcutsa sözleşmeye nullable tutar ve para birimi (ör. `prizeAmount`, `prizeCurrency`) eklenmeli; yalnız sonuçlanmış programda ve pozitif/geçerli tutarda Sonuçlar kartında `İkramiye` olarak para formatıyla gösterilmelidir. Eksik değer `0` gibi yorumlanmamalı, tahmin/main hit/kapsam sayısından tutar türetilmemeli ve alan yoksa bölüm tamamen gizlenmelidir. Kaynak SAP/Toto alanı, OData adı ve para birimi cutover öncesi ilgili `BTB Toto - Aktif` sahipliğinde doğrulanmalı; Mobile tarafı yalnız sabit read-only DTO'yu göstermelidir. | MEDIUM | READY |

| NXT-OBS-091 | 2026-08-13 | Karar Günlüğü yıldız sıralaması | Karar Günlüğü, mevcut durum/gün/yıldız filtrelerinden bağımsız olarak yıldız derecesine göre artan ve azalan sıralanabilmeli. Üst satıra yeni bir chip ekleyerek dar ekranı sıkıştırmak yerine mevcut yıldız açılır menüsü iki açık bölüme ayrılmalı: `Yıldız filtresi` ve `Sıralama`. Sıralama seçenekleri `Varsayılan` (ek yıldız sıralaması uygulamadan ekranın bugün kullandığı mevcut/en yeni karar sırasına kesin dönüş), `Yıldız: yüksekten düşüğe` ve `Yıldız: düşükten yükseğe` olmalı; yalnız aktif yıldız sıralamasında trigger üzerinde küçük fakat renge bağlı olmayan yukarı/aşağı ok görünmelidir. Kullanıcı sıralamayı temizlediğinde veya `Varsayılan`ı seçtiğinde liste hemen mevcut doğal sırasına dönmelidir. Yıldız sıralaması yalnız mevcut gün kapsamı, durum tabı ve yıldız filtresinden kalan sonuçlara uygulanmalı; eşit yıldızlı kararlarda kararlı ikincil sıra en yeni karar önce olmalıdır. Seçim listeyi yeniden fetch etmeden yerel olarak sıralamalı, scroll konumunu kontrollü biçimde başa almalı, yıldız ve gün menüleri aynı anda açık kalmamalı, 44–48 dp hedef/Android geri/dışarı dokunma/büyük yazı ve dar ekran davranışları korunmalıdır. | MEDIUM | READY |

| NXT-OBS-092 | 2026-08-13 | Canlı güncel baskı farkı ve detay dengesi | Canlı Maçlar kartındaki baskı farkı tarihsel Super karar değerinden değil en son güncel maç snapshot'ından gelmeli ve açıkça `güncel baskı farkı` olarak etiketlenmelidir. Mevcut Mobile DTO `pressureDiff` / `totalPressure` alanlarını BFF'de `last_pressure_diff` / `last_total_pressure` üzerinden mapliyor; fiziksel cihaz gözleminde gösterilen değerin güncel olmadığı görüldüğü için cutover'da `Mobile liste/detay -> BFF mapper -> OData entity/property -> SAP current match source` zinciri doğrulanmadan yalnız etiket değiştirilmemelidir. Canlı maç detayında ortak `PressureBalance` düzeni yeniden kullanılmalı, başlığı `Güncel baskı dengesi` olmalı ve yalnız current-match `totalPressure` / `pressureDiff` snapshot'ından beslenmelidir. Super detayındaki `Karar anı baskı dengesi` tarihsel Super Log snapshot'ı olarak aynen kalmalıdır. Güncel veri eksik veya stale ise sıfır uydurulmamalı; `Veri bekleniyor` ya da stale açıklaması gösterilmelidir. Liste ve detay aynı değer, yuvarlama, yön ve zaman damgası semantiğini kullanmalı; pull-to-refresh/query refresh sonrası birlikte güncellenmelidir. | HIGH | READY |

| NXT-OBS-093 | 2026-08-13 | Toto tamamlanan maçlarda biten skor | Toto Programı detayındaki Tahminler listesinde sonuçlanan her maç için yalnız `Sonuç 1/X/2` değil biten skor da gösterilmelidir. Mevcut Mobile Toto fixture/prediction sözleşmesi yalnız nullable `actualResult` taşıyor; ev/deplasman biten skor alanı bulunmuyor. Cutover'da ilgili `BTB Toto - Aktif` veri sahipliğinde gerçek sonuç kaynağı doğrulanarak Mobile BFF DTO'suna birbirine bağlı nullable `homeScore` / `awayScore` alanları eklenmeli ve yalnız iki değer de mevcut, geçerli ve maç sonuçlanmışsa kartın sağ sonuç alanında kompakt `Skor 2-1` biçiminde gösterilmelidir. `actualResult` ana tahmin/kuponda/kapsam dışı sınıflandırmasının ayrı kaynak değeri olarak korunmalı; skor üzerinden sessizce yeniden hesaplanmamalıdır. Skor eksikse sahte `0-0` üretilmemeli ve mevcut `Sonuç 1/X/2` görünümü korunmalıdır. Dar ekran, büyük yazı ve uzun takım adlarında skor ile sonuç etiketi çakışmamalı; BFF şema/mapper/testleri ile Mobile şema/render testleri birlikte doğrulanmalıdır. | MEDIUM | READY |

| NXT-OBS-094 | 2026-08-13 | Ana menüler arasında yatay kaydırma | Alt navigasyondaki ana ekranlar `Özet -> Canlı -> Super -> Toto -> Daha Fazla` sırasıyla yatay parmak hareketiyle de değiştirilebilmelidir: sola kaydırma bir sonraki, sağa kaydırma bir önceki ana sekmeye götürmelidir. Davranış yalnız `(tabs)` ana ekranlarında etkin olmalı; detay ekranlarındaki sol-kenardan geri jesti aynen korunmalı ve iki jest aynı ekranda yarışmamalıdır. Yatay yön baskın değilse veya kullanıcı dikey liste kaydırıyorsa gesture yakalanmamalı; filtre menüsü, buton, link ve yatay etkileşimli alanlarda yanlış sekme değişimi önlenmelidir. Geçiş mevcut Expo Router tab durumunu, filtreleri ve mümkünse liste scroll konumunu korumalı; ilk/son sekmede wrap yapmamalı, kontrollü direnç gösterip yerinde kalmalıdır. Mesafe ile hız eşiği fiziksel cihazda tek elle kullanım için doğrulanmalı; kısa/yanlış hareket geri yaylanmalıdır. Alt menü aktif durumu hareket tamamlanınca yeni ekranla aynı anda değişmeli, açık/koyu tema ve reduced-motion erişilebilirlik ayarı korunmalı, ekran okuyucuda mevcut alt sekme navigasyonu birincil ve eksiksiz kalmalıdır. Android dikey scroll, pull-to-refresh, Bibi sürükleme/rehber, klavye ve sistem geri davranışları için regresyon testi yapılmalıdır. **Sınıflandırma ve yerel çözüm (2026-08-23, `mobile cutover start` batch):** Denetimde `src/components/Screen.tsx` + `src/navigation/tab-swipe.ts`'in bu maddenin tanımladığı davranışı zaten tamamen uyguladığı görüldü — beş ana sekmenin hepsi `tabSwipe` ile bağlı, yön/eşik/hız mantığı saf ve test edilmiş (`tab-swipe.test.ts`), ilk/son sekmede wrap yerine `resistanceRatio` direnci var, `localTabSwipe` iç sekmeye önce hak tanıyor (`NXT-OBS-106`), ve tab-swipe ile edge-back jesti `Screen` başına karşılıklı dışlanıyor. Tek gerçek eksik: geri yaylanma (`withSpring`) `useReducedMotion`/`gestureSpring` ortak deseninden (`CollapsibleModule`, `ChangeEmphasis`, `LiveDot`) geçmiyordu, kendi yerel `springBack` sabitini kullanıyordu. `Screen.tsx` artık `useReducedMotion()` okuyor; azaltılmış hareket açıkken commit ve iptal sonrası yaylanma yerine anında `0`'a iniyor, aksi halde paylaşılan `theme.gestureSpring` kullanılıyor. Yerel `springBack` kopyası kaldırıldı. `npm run check` (tsc/eslint/421 test/brand) temiz geçti; fiziksel cihaz doğrulaması yapılmadı, yeni APK üretilmedi (kapsam tek dosyalık erişilebilirlik düzeltmesi). | MEDIUM | RESOLVED |

| NXT-OBS-075 | 2026-08-11 | Bibi rehberi ayarları | Daha Fazla ekranında yalnız `Bibi rehberini baştan başlat` eylemi bırakıldı; yinelenen durum satırı kaldırıldı. Android açık tema smoke’unda tek eylem düzeni doğrulandı. | LOW | READY |

| NXT-OBS-076 | 2026-08-11 | Genel UI düzeni ve insan-etkileşim standardı | Ortak `Screen`, filtre, bölüm başlığı ve metrik kartları 44–48 dp dokunma hedefi, 720 dp içerik sınırı, güvenli alan, tutarlı spacing/tipografi ve dar ekranda wrap kurallarıyla düzenlendi. Super ve Maç detay metrikleri okunabilir iki kolon ritmine taşındı; standart `docs/UI_INTERACTION_STANDARD.md` altında yazılı kabul kapısı oldu. Android açık tema emülatöründe giriş, Özet ve Maç Detayı görsel/dokunmatik smoke geçti. | HIGH | READY |

| NXT-OBS-100 | 2026-08-18 | Doğal kırmızı kart gözlemi (release engeli değil) | Gerçek bir kırmızı kart doğal olarak oluştuğunda `DIRECT_RED` / `SECOND_YELLOW_RED`, skor gösterimi ve sarı kart dışlama semantiği gözlemsel doğrulanır. Kırmızı kart uydurulmaz; doğal olay yokluğu kod batch’i başlatmaz. | LOW | DEFERRED |
| NXT-OBS-101 | 2026-08-18 | Kanonik BTB marka varlığı — **KAPANDI** | **KODDA ÇÖZÜLDÜ (2026-08-19).** Kare zemin raster'ın içindeydi (192x192, tam saydam piksel 0, köşeler `(0, 8, 27, α=221)`) ve kodda kapatılamıyordu. Üretilen Intelligence Noir master'ı doğrulandı (gerçek saydamlık, beyaz saçak yok, kimlik yerinde), deterministik olarak 1024² saydam tuvale normalize edildi ve iki role türetildi: `btb-mark.png` (%92) ve `btb-adaptive-foreground.png` (%62 güvenli bölge). `expo prebuild` sonrası `ic_launcher_foreground` 0 saydam pikselden **%79** saydama, köşe alfası 221'den **0**'a geçti; bildirilen `#04101E` adaptive arka plan artık görünüyor. Açılış ekranındaki `borderRadius: 30` telafisi aynı değişiklikte kaldırıldı. `npm run check:brand` iki rol için de PASS ve `npm run check` kapısına eklendi. 18 dp dahil küçük boy siluet doğrulaması geçti. Fiziksel Xiaomi doğrulaması 2026-08-19'da geçti. | HIGH | RESOLVED |
| NXT-OBS-102 | 2026-08-18 | Canlı kart baskı etiketi (fiziksel Xiaomi) | `güncel baskı farkı` 360dp satırda sayının yanında taşıyordu. Etiket `baskı farkı` oldu; güncellik `pressureSource === "CURRENT_MATCH"` kapısıyla kodda zorlandığı ve canlı kart zaten şimdiki zaman yüzeyi olduğu için anlam kaybı yok. `güncel veri bekleniyor` kısaltılmadı, kontrollü ikinci satıra alındı. Emülatörde 360dp koyu ve açık temada doğrulandı (`f3d628d`); fiziksel Xiaomi doğrulaması bir sonraki APK ile. | MEDIUM | READY |
| NXT-OBS-103 | 2026-08-19 | Marka varlığı fiziksel doğrulaması — **KAPANDI** | Yeni marka launcher ikonu, açılış ekranı, Özet hero ve takım arması fallback'i olmak üzere dört yüzeyi birden değiştiriyor. Emülatörde release APK ile doğrulananlar: launcher ikonu daire maskede tam (kalkan kırpılmıyor, `#04101E` arka plan görünüyor), açılış ekranında kare/yuvarlatma/köşe kırpılması yok, Özet hero koyu ve açık temada kutusuz. Sahip fiziksel Xiaomi doğrulamasını 2026-08-19'da tamamladı ve marka entegrasyonunu kabul etti; Milestone 2 kapandı. | HIGH | RESOLVED |
| NXT-OBS-104 | 2026-08-21 | SAP bridge SICF dayanıklılığı ve runtime görünürlüğü | Live Context iki canlı maçta `404 / Service cannot be reached` verdi; Mobile dürüst unavailable gösterdi. Owner `/default_host/sap/bc/zbet/provider/detail` düğümünü `ZBET_CL_PROVIDER_BRIDGE` handler’ıyla geri açtı; salt-okunur kanıt kısa süre `401` (tokensız), `200 Success` (tokenlı) ve BFF `availability=OK` gösterdi, daha sonra aynı uç yeniden `404` oldu. Yerel `zbet-abap`ta handler sınıfı var fakat SICF nesnesi sürüm kontrollü değil; kalıcı çözüm live SAP düğümünü abapGit’e almak ve tekrar aktivasyon/read-back yapmaktır. Bu batch, ek upstream isteği üretmeden gerçek maç çağrılarında zaten görülen son outcome/HTTP status/counter değerlerini `/health.liveContextTelemetry` altında yayımlayan BFF sertleştirmesini yerel olarak tamamladı; deploy ve SAP işlemi ayrı onaydır. | HIGH | READY |
| NXT-OBS-105 | 2026-08-21 | Super Detayı canlı sonuç bandı | Fiziksel Xiaomi ekranında maç sürerken `Super Kararı Detayı > SONUÇ` bandı `— / sonuç bekleniyor` gösteriyor. Maç canlıysa bu alanda tarihsel karar anı skoru yerine current-match kaynağından gelen **güncel canlı skor ve dakika** birlikte gösterilmeli (ör. `1 - 2 · 78′ canlı`); böylece üstteki `KARAR ANI · 70′` skoru ile o anki maç durumu açıkça ayrılmalıdır. Maç bittiyse mevcut güvenilir biten skor/kâr semantiği, void ise mevcut void davranışı korunmalı; current-match skor veya dakika eksik/stale ise karar snapshot’ından değer türetilmemeli ve mevcut `sonuç bekleniyor` fallback’i kalmalıdır. Dar ekran, büyük yazı ve açık/koyu tema doğrulanmalı; mümkünse erişilebilirlik metni skoru ve dakikayı birlikte okumalıdır. **Yerel çözüm (2026-08-21):** Super logun `matchKey` değeriyle current-match sorgusu eklendi; yalnız `LIVE/HALF_TIME` durumunda güncel skor+dakika, biten kararda güvenilir final skor, aksi durumda güvenli bekleme fallback'i gösteriliyor. Otomatik test ve ARM64 native paketleme geçti. Owner aynı gün ARM64 aday APK'nın fiziksel Xiaomi testini tamamlayıp bu satırın görsel parity kapısını kabul etti. | HIGH | RESOLVED |
| NXT-OBS-106 | 2026-08-21 | Yatay kaydırmada iç sekme önceliği | Fiziksel Xiaomi kullanımında ana ekranlar arasında yatay kaydırma çalışıyor; ancak sayfada yönle uyumlu bir iç sekme varsa önce o sekme değişmelidir. Görsel örneklerde Canlı ekranındaki `Canlı / Tümü` ve Super Karar Günlüğü’ndeki `Tümü / Açık` sırası bu kurala dahildir: sola kaydırma görünür sıradaki sonraki iç sekmeye, sağa kaydırma önceki iç sekmeye gider. Kullanıcı ilgili yönde iç sekmenin sınırındaysa — veya sayfada iç sekme yoksa — aynı jest `Özet -> Canlı -> Super -> Toto -> Daha Fazla` ana tab sırasını değiştirir. Tek jest aynı anda hem iç hem ana tabı değiştirmemeli; filtre dropdown/chip’leri sekme sayılmamalıdır. İç sekme seçimi mevcut filtre/scroll sözleşmesini korumalı; yatay yön baskın değilse dikey scroll, pull-to-refresh, kart/buton dokunuşu ve detay ekranındaki sol-kenar geri jesti önceliğini kaybetmemelidir. Davranış dar ekran, açık/koyu tema, reduced-motion ve erişilebilirlik ile fiziksel cihazda doğrulanmalıdır. **Yerel çözüm (2026-08-21):** Ortak `Screen` jesti aynı yöndeki görünür yerel sekmeyi ana tabdan önce seçiyor; sınırda ana navigasyona devrediyor ve tek jestte yalnız tek seviye değişiyor. `STAR`/dropdown durumları yerel sekme sırasına alınmadı. Birim testleri ve ARM64 native paketleme geçti. Owner aynı gün ARM64 aday APK'nın fiziksel Xiaomi testini tamamlayıp jest parity kapısını kabul etti. | HIGH | RESOLVED |
| NXT-OBS-107 | 2026-08-21 | Gol ve kırmızı kart maç akışı tasarımı | `Maç Detayı > Goller ve kırmızı kartlar` modülündeki düz satırlar dakika, ev/deplasman tarafı ve olay sonrası skor ilişkisini yeterince güçlü göstermiyor. Referans görseldeki maç akışı fikri BTB tasarım diline özgün biçimde uyarlanmalı: ortada kesintisiz bir dikey zaman rayı, ray üzerinde belirgin dakika düğümü, ev olayları bir yanda ve deplasman olayları karşı yanda yer almalı; gol için top simgesiyle doğrulanmış olay sonrası skor güçlü bir rozet olarak, kırmızı kart için kart simgesi ve takım/EV–DEP bağlamı açıkça gösterilmelidir. Olaylar maç kronolojisinde kalmalı ve en güncel olay görsel olarak seçilebilmeli; taraf ayrımı yalnız renge dayanmamalıdır. Kaynakta doğrulanmış oyuncu adı veya olay-anı skoru varsa gösterilebilir, yoksa takım adı ve mevcut olay verisiyle yetinilmeli; skor yeniden oluşturulmamalı, oyuncu adı tahmin edilmemeli ve sarı kart/değişiklik gibi `btb.live-context.v2` kapsamı dışındaki olaylar geri eklenmemelidir. Uzun takım/oyuncu adları, uzatma dakikası (`90+4′`), art arda aynı dakikadaki olaylar, kırmızı kartta eksik skor, boş/stale/unavailable durumları, ekran okuyucu sırası, dar ekran ve açık/koyu tema doğrulanmalıdır. **Yerel çözüm (2026-08-21):** Modül BTB'ye özgü merkezi zaman rayı, ev/deplasman şeritleri, dakika düğümü, doğrulanmış skor rozeti, açık olay/taraf etiketi ve son olay vurgusuyla yenilendi; mevcut kaynak dışından skor ya da oyuncu türetilmiyor. Otomatik kalite ve ARM64 paketleme geçti. Owner aynı gün ARM64 aday APK'nın fiziksel Xiaomi testini tamamlayıp koyu/açık tema parity kapısını kabul etti. | MEDIUM | RESOLVED |
| NXT-OBS-108 | 2026-08-21 | Toto program listesinde teorik ikramiye göstergesi | Toto Programları listesinde teorik ikramiye kazancı bulunan sonuçlanmış programlar, diğer programlardan küçük bir para işareti ikonuyla ayrılmalıdır. Gösterge kart hiyerarşisini bozmamalı; `SONUÇLANDI` rozeti, program başlığı, Jinx katmanı ve dar ekran yerleşimiyle çakışmamalıdır. TRY semantiğinde `$` yerine küçük `₺`/para ikonu kullanılmalı ve ekran okuyucu etiketi açıkça `Teorik ikramiye var` demelidir. İkon yalnız DTO/sözleşmedeki güvenilir `theoreticalPrize` sonucunun gerçekten pozitif teorik kazanç bildirdiği programlarda görünmeli; `ana isabet`, `kapsama isabet` veya kolon maliyetinden kazanç uydurulmamalıdır. Teorik tutar gerçek/ödenmiş ikramiye gibi adlandırılmamalı; liste göstergesi yalnız teorik kazanç işaretidir. İkramiye olmayan, sonuçlanmamış veya veri bulunmayan programların kartı bugünkü görünümünü korumalıdır. Açık/koyu tema, renk körlüğü, erişilebilirlik ve fiziksel telefon görünümü doğrulanmalıdır. **Sahip kararı (2026-08-24):** Önceki yalnız doğrulanmış gerçek ödeme göstergesi şartı bu liste ikonu için geri alındı; sahip açıkça teorik ikramiye kazanan programların küçük para ikonuyla gösterilmesini istedi. Referans: sahibin 2026-08-24 tarihinde paylaştığı `Photo 1.jpg`. Gerçek ödeme alanı gerektiren ayrı kapsam hâlâ bu ikon tarafından çözülmüş sayılmaz. **Yerel batch (2026-08-24):** `RESULTED` ve pozitif `theoreticalPrize` koşulu ortak saf yardımcıyla bağlandı; kart durum rozetinin altında semantik-pozitif küçük `₺` ikonu ve `Teorik ikramiye var` ekran okuyucu metni gösteriyor. Sonuçlanmamış, sıfır veya eksik değerde ikon yok. Android 15 emülatöründe gerçek Program 350/349/348 satırlarında yerleşim doğrulandı; otomatik test ve x86_64 native build geçti. Fiziksel telefon/sahip kabulü bekleniyor. | MEDIUM | READY |
| NXT-OBS-109 | 2026-08-21 | Canlı oran yokken kickoff oranı görünürlüğü | `Maç Detayı > Oran sonuçları` satırlarında maç başlamış fakat ilgili canlı fiyat henüz gelmemişken yalnız `Canlı oran kapalı` yazıyor. Aynı market/seçim için doğrulanmış kickoff oranı mevcutsa bu alanda açık kaynak etiketiyle gösterilmeli (ör. `Kick-off 1,85` veya dar ekranda `KO 1,85`); canlı oran geldiğinde öncelik canlı fiyata dönmelidir. Kickoff oranı hiçbir zaman güncel/bahse açık canlı fiyat gibi renklendirilmemeli veya adlandırılmamalı, odds movement hesabına katılmamalı ve Super seçimi/model girdisi/piyasa açık-kapalı durumunu değiştirmemelidir. Gerçekten kapanmış ya da kaybolmuş canlı markette kickoff fiyatı yalnız tarihsel referans olabilir; `canlı oran kapalı` semantiğini gizlememeli, gerekirse `Canlı kapalı · KO 1,85` birlikte gösterilmelidir. Fallback yalnız aynı kanonik market ve seçim anahtarının güvenilir kickoff alanından gelmeli; başka marketten, stale `live_rate` kaydından veya olasılık yüzdesinden oran türetilmemelidir. Kickoff oranı da yoksa mevcut güvenli boş/kapalı durum korunmalı; uzun market adları, ondalık biçim, dar ekran, açık/koyu tema ve erişilebilirlik açıklaması doğrulanmalıdır. **Sınıflandırma (2026-08-21):** Mevcut market DTO'su yalnız `liveRate` taşıyor. `RatioResult.kickOff` fiyat değil tarihsel sonuç yüzdesi olduğundan oran diye yeniden etiketlenmedi. Aynı kanonik seçim için doğrulanmış kickoff fiyatı sözleşmeye eklenene kadar ertelendi. **Çözüldü (2026-08-23, `mobile cutover start` batch):** Web `Match Odds > Odd` kaynağı yerel CDS/OData metadata zincirinde `zbet_cds_001x`, `type='01'` olarak doğrulandı; BFF yalnız aynı marketin yayınlanan maç önü alanını nullable `kickoffRate` olarak taşıyor. Mobile canlı oranı önceliklendiriyor; Kick-Off aşamasında canlı yoksa `Kick-off oranı`, sonraki aşamalarda canlı kapanmışsa `Canlı kapalı · KO` etiketiyle tarihsel referans gösteriyor. `Ms1X`, `MsX2`, `Ms05u` gibi bu entity'de yayınlanmayan marketlerde oran türetilmiyor. SAP yazısı/aktivasyonu yapılmadı. | HIGH | RESOLVED |
| NXT-OBS-110 | 2026-08-21 | Bibi kimliğinin Jinx olarak yenilenmesi | Sahip kararı: Mobile Next’in kullanıcıya görünen yardımcı karakter adı `Bibi` yerine **`Jinx`** olmalıdır. Ayarlar, rehber, ipuçları, konuşma balonları, erişilebilirlik metinleri ve kullanıcıya görünen diğer tüm adlandırmalar tutarlı biçimde `Jinx`e geçmelidir. Yeni görsel kimlik, sevilen asi/zeki/enerjik karakter hissini Intelligence Noir diline çevirmeli; elektrik mavisi/teal enerji, asimetrik hareket, güçlü mimik ve sinematik dokulu derinlik kullanılabilir, ancak Arcane karakterinin yüzü, uzun örgüleri, kıyafeti, dövmeleri, silahları veya özgün sembolleri birebir kopyalanmamalıdır. Jinx özgün bir BTB futbol zekâ asistanı olarak kalmalı; mevcut şeffaf runtime frame rolleri, drag/menu/tutorial/discovery, `GUIDE_ONLY`, reduced-motion ve tek-sürekli-animasyon kuralları korunmalıdır. Yeniden adlandırma yalnız gerekli kullanıcı yüzeylerini değiştirmeli; mevcut kurulumların tutorial/discovery ilerlemesini, sessiz modunu ve konum tercihlerini tutan teknik/storage anahtarları sırf ad değişti diye kırılmamalı veya sıfırlanmamalıdır. Yeni bitmap/frame varlıkları ayrı üretim brief’i, şeffaflık/ölçek doğrulaması ve koyu-açık tema fiziksel cihaz kontrolü gerektirir. **Yerel çözüm (2026-08-21):** Kullanıcıya görünen adlar `Jinx` oldu; teknik/storage `bibi` anahtarları göç uyumu için korundu. Özgün BTB futbol-orb kimliğinde open/half/closed RGBA frame'leri üretildi, köşelerde alfa en çok 1 olarak doğrulandı ve runtime'a bağlandı. Otomatik marka/uygulama kontrolü ile ARM64 paketleme geçti. Owner aynı gün ARM64 aday APK'nın fiziksel Xiaomi testini tamamlayıp Jinx'in koyu/açık tema ve hareket parity kapısını kabul etti. | MEDIUM | RESOLVED |
| NXT-OBS-111 | 2026-08-22 | Kesintisiz, sayfa-izleyen ekran geçişleri | **Çözüldü (2026-08-22):** Ana tab sahneleri fade olmadan tam ekran genişliğinde karşılıklı kayıyor; komşu sahneler önceden bağlı, navigator zemini BTB arka planında ve alt bar sabit. Detail stack ileri/geri yönlü ortak slide kullanıyor; ortak jest commitinde eski iç yaylanma/stack çift animasyonu kaldırıldı, reduced-motion none fallback korunuyor. Android 15 ara-frame kanıtı iki sahneyi aynı anda ve alt barı sabit gösterdi; sahip paylaşılan görsel kanıtı kabul etti. | HIGH | RESOLVED |
| NXT-OBS-112 | 2026-08-22 | Super tercihlerinin maç timeline içine alınması | **Çözüldü (2026-08-22):** Aynı maça ait Super kararları gol ve kırmızı kartlarla yalnız sunum katmanında dakika artan tek akışa birleşti; tarafsız altın yıldız düğümü dakika, rating ve seçimi gösterip `/super/[key]` detayına açılıyor. Ayrı `relatedSuper` modülü ve kalıcı düzen kimliği emekliye ayrıldı; son olay vurgusu ve kaynak ayrımı korundu. Pure kronoloji ve migration testleri ile Android 15 dolu timeline smoke geçti; sahip paylaşılan görsel kanıtı kabul etti. | HIGH | RESOLVED |
| NXT-OBS-113 | 2026-08-22 | Karar Günlüğü açık-karar switch filtresi | **Çözüldü (2026-08-22):** `Açık` tabı kaldırıldı; ekran-yerel ve varsayılan kapalı `Yalnız açık {count}` switch yalnız `OPEN` kararları geçiriyor, Tümü, yıldız ve gün kapsamıyla bağımsız birleşiyor, refetch sırasında disabled ve eski yerel tab swipe yok. Kombinasyon testleri ve Android 15 üzerinde `3 -> 1` filtre smoke geçti; sahip paylaşılan görsel kanıtı kabul etti. | HIGH | RESOLVED |
| NXT-OBS-114 | 2026-08-22 | Canlı ekranında `Tümü` yerine yaklaşan `Fikstür` | **Çözüldü (2026-08-22):** `Tümü` yerine yalnız `status === "NOT_STARTED"` kabul eden `Fikstür` geldi; kickoff sırası korunuyor, eski `scope/filter=ALL` bağlantıları `FIXTURE` kapsamına normalize ediliyor ve özel boş durum kullanılıyor. Filtre ve legacy-link testleri ile Android 15 üzerinde yalnız başlamamış kart smoke geçti; sahip paylaşılan görsel kanıtı kabul etti. | HIGH | RESOLVED |
| NXT-OBS-115 | 2026-08-22 | Super Karar Günlüğü kayıt sınırı 100 → 200 | **Çözüldü (2026-08-22):** Ana `/v1/super/logs` SAP okuması ile match-key zenginleştirmesi `$top=200`; OpenAPI `maxItems: 200`. Gerçek 200 fixture testi sıra, kayıpsız map ve güvenli URL uzunluğunu doğruladı; exact-match geçmişi `$top=50`, dashboard `recentSuper` küçük sınırı ve KPI davranışı korunuyor. BFF full test ve production build geçti. CAP `0832b16` commit/push edildi; temiz `19aa8f8` pilot runtime read-back'inde public `/v1/super/logs` tam 200 satır döndürdü. Fiziksel 200 satır kaydırması yapılmadı; sahip teknik sözleşme/test ve public read-back kanıtını kabul etti. | MEDIUM | RESOLVED |
| NXT-OBS-116 | 2026-08-22 | Jinx çevre halkasının kaldırılması ve görselin büyütülmesi | **Çözüldü (2026-08-22):** Ayrı halo view, stil ve opacity animasyonu kaldırıldı; şeffaf Jinx frame varlıkları değişmeden görsel ve anchor birlikte `58 -> 68dp` büyüdü. Clamp, drag, menu, bubble ve `btb-mobile-next-bibi-position-v3` saklama sözleşmesi ortak ölçüden devam ediyor. Android 15 koyu tema ve safe-area görünümü çembersiz geçti; sahip paylaşılan görsel kanıtı kabul etti. | MEDIUM | RESOLVED |
| NXT-OBS-121 | 2026-08-24 | Mobilde açılan BTB web başlığındaki `BTB WEB` rozetinin kaldırılması | Sahibin paylaştığı fiziksel telefon görselinde BTB web uygulama kartlarının üstünde, sağ üstte koyu kapsül biçiminde `BTB WEB` rozeti görünüyor. Sahip bu yazı/rozetin tamamen kaldırılmasını istiyor. Yalnız sağ üstteki `BTB WEB` kontrolü kaldırılmalı; soldaki `BTB` sayfa başlığı ile `Better Than Bet`, `Super Log Report`, `BTB Toto` ve `BTB Admin` kartları korunmalıdır. Cutover sırasında gerçek sahip ekran/bileşen bulunmalı; kaldırma sonrasında başlık satırında boşluk veya hizalama artığı kalmadığı, safe-area'nın ve Jinx katmanının bozulmadığı, açık/koyu tema ile fiziksel telefon görünümünün doğru olduğu doğrulanmalıdır. Referans: sahibin 2026-08-24 tarihinde paylaştığı `Photo 1.jpg`. **Yerel batch (2026-08-24):** Mobile Fiori kabuğundaki sağ-üst opsiyonel `BTB WEB` rozeti ve stilleri kaldırıldı; WebView hata/fallback yüzeyi korunuyor. Android 15 emülatöründe Work Zone giriş yüzeyi açıldı, rozet boşluğu kalmadığı ve Jinx/safe-area katmanlarının bozulmadığı doğrulandı. Kaynak sözleşme testi ve x86_64 native build geçti; fiziksel telefon/sahip kabulü bekleniyor. | MEDIUM | READY |
| NXT-OBS-122 | 2026-08-24 | Güncel maç görünümünde ayrı `Aç` butonunun durum alanına taşınması | Güncel maç görünümündeki bağımsız `Aç` butonu kaldırılmalıdır. Butonun bugün çalıştırdığı aynı detay açma/navigasyon eylemi, kartta mevcut duruma göre `Sonuç` veya `Canlı oynanırken` yazan durum alanına dokunulduğunda çalışmalıdır; hedef route ve mevcut uygunluk/guard kuralları değiştirilmemelidir. Yalnız metin değil, durum alanının tamamı en az 44–48 dp erişilebilir dokunma hedefi olmalı; pressed feedback, `button` rolü ve detayın açılacağını söyleyen erişilebilirlik ipucu taşımalıdır. `Aç` kaldırıldığında kartta boş hizalama artığı kalmamalı; canlı/sonuç renk ve metin semantiği korunmalı, kartın diğer dokunma ve swipe davranışlarıyla çift navigasyon oluşmamalıdır. Dar ekran, büyük yazı, açık/koyu tema, Android geri dönüşü ve fiziksel telefon akışı doğrulanmalıdır. Referans: sahibin 2026-08-24 tarihinde paylaştığı `Super Kararı Detayı` görseli. **Yerel batch (2026-08-24):** Ayrı `Güncel maç görünümünü aç` butonu kaldırıldı; SONUÇ bandının skor/durum hücresi 44–48 dp dokunma hedefi, pressed feedback, `button` rolü ve açık erişilebilirlik ipucuyla aynı current-match route'una bağlandı. Android 15 emülatöründe Super karar detayında ayrı butonun bulunmadığı ve `sonuç bekleniyor` alanına dokununca `Maç Detayı`na gidildiği doğrulandı. Kaynak sözleşme testi ve x86_64 native build geçti; fiziksel telefon/sahip kabulü bekleniyor. | MEDIUM | READY |
| NXT-OBS-123 | 2026-08-24 | Canlı ekranında yalnız Canlı switch'i ve sabitlenen maçlar | Sahibin fiziksel Xiaomi görselindeki üst `Canlı / Fikstür` seçimi tek ve kompakt bir segment switch görünümüne dönüştürülmelidir. Aynı Canlı ekranına üçüncü yerel görünüm olarak yalnız dürbün ikonuyla temsil edilen `Scout` eklenmelidir; görünür `Scout` metni şart değildir ancak ikonun erişilebilir adı ve seçili durumu açık olmalıdır. Canlı/Fikstür maç kartlarında dürbün kontrolü favori toggle mantığıyla maçı Scout'a ekleyip çıkarmalı; seçili durum yalnız renkle değil dolu/aktif ikon veya eşdeğer erişilebilir durumla anlaşılmalıdır. Scout görünümü yalnız kullanıcının dürbünle işaretlediği ve güncel Canlı/Fikstür akışında hâlâ bulunan maçları göstermeli; maç kaynak listeden düştüğünde Scout'ta da görünmemelidir. Tercih cihaz içinde küçük bir güncel seçim kümesi olarak tutulabilir ve mevcut feed ile uzlaştırılmalıdır; tarihçe/audit log üretilmemeli, BFF/SAP'a yazılmamalı, notification/model davranışına bağlanmamalı ve Super ekranında ikon, filtre veya Scout görünümü bulunmamalıdır. Boş durumda kısa bir yönlendirme gösterilmeli; mevcut yatay pager, tap/swipe önceliği, yıldız filtresi, maç detayına geçiş, liste performansı, dar ekran, büyük yazı, reduced-motion ve açık/koyu tema korunmalıdır. Referans: sahibin 2026-08-24 tarihinde paylaştığı Canlı maçlar `Photo 1.jpg`. **Yerel batch (2026-08-24):** `Canlı / Fikstür / Scout` tek kompakt segment oldu; bağımsız yıldız filtresi ve dört sayfalı native pager korundu. Canlı/Fikstür/Scout kartlarında erişilebilir dürbün toggle'ı, aktif check işareti ve yalnız başarılı güncel feed ile uzlaşan bounded cihaz-içi seçim kümesi eklendi; nested press maç detayını açmıyor ve hiçbir BFF/SAP yazısı üretmiyor. Android 15 emülatöründe gerçek canlı maç seçimi Scout sayacını `0 -> 1` yaptı ve Scout sayfası yalnız seçilen maçı gösterdi. Resmî Mobile kalite kapısı geçti. **Sahip düzeltmesi / yeniden açılış (2026-08-24):** Uygulanan çoklu segment ve Scout yönü kabul edilmedi. Segment alanında yalnız `Canlı` switch'i kalmalı; `Fikstür` ve `Scout` segmentleri, Scout sayfası ve maç kartlarındaki dürbün kontrolleri kaldırılmalıdır. Maç kartına bunun yerine erişilebilir, seçili durumu yalnız renge bağlı olmayan bir sabitleme ikonu eklenmelidir. İkona basmak maç detayını açmamalı; maçı e-posta uygulamalarındaki sabitleme davranışı gibi, yalnız en az bir seçim varken listenin başında görünen `Sabitlenenler` bölümüne taşımalıdır. Sabitleme kaldırılınca maç kendi doğal grup ve sırasına dönmeli; diğer maçlar aşağıdaki normal akışını korumalıdır. Seçim cihaz içinde küçük bir güncel küme olarak saklanıp başarılı güncel feed ile uzlaştırılmalı; kaynak listeden düşen maç gösterilmemeli, tarihçe tutulmamalı ve BFF/SAP/model/notification davranışı değiştirilmemelidir. Mevcut maç detayı, yıldız filtresi, pager/gesture önceliği, dar ekran, büyük yazı, reduced-motion ve açık/koyu tema korunmalıdır. Önceki yerel Scout taslağı çalışma ağacında bulunduğundan yeni cutover'da temiz biçimde dönüştürülmeli ve yeniden kalite/görsel doğrulamadan geçirilmelidir. **Sahip düzeltmesi yerel batch (2026-08-24):** Scout görünümü, dürbün kontrolleri ve çoklu segment tamamen kaldırıldı. Üst yüzeyde yalnız `Canlı` switch'i ile bağımsız yıldız filtresi kaldı; switch kapalıyken mevcut Fikstür sayfasına, açıkken Canlı sayfasına gider. Karttaki erişilebilir pin kontrolü maçı detaya gitmeden yalnız doluyken görünen `Sabitlenenler` grubuna taşır; kaldırınca doğal kickoff grubuna döndürür. Bounded cihaz-içi küme yalnız başarılı güncel feed ile uzlaşır ve BFF/SAP/model/notification yazısı üretmez. Android 15 gerçek API smoke'unda sabitleme, en üste taşıma, geri alma ve doğal sıraya dönüş doğrulandı; Scout/dürbün kaynağı kalmadı. Resmî Mobile kalite kapısı geçti. | MEDIUM | READY |
| NXT-OBS-124 | 2026-08-24 | Super karar detayında tıklanabilir Sonuç göstergesi | Fiziksel Xiaomi görselinde `SONUÇ` bandı mevcut durumda statik bilgi alanı gibi okunuyor; güncel maç detayına açılabildiği yeterince belli değil. Bandın mevcut tam-genişlik dokunma hedefi ve aynı current-match route'u korunmalı, `SONUÇ` başlık satırının sağ ucuna küçük ve ikincil tonda bir sağ şevron eklenmelidir. Şevron yalnız görsel affordance olmalı, ayrı iç içe buton veya ikinci navigasyon üretmemeli; skor/kâr hizasını daraltmamalı ve kaybedilen/kazanılan renk semantiğiyle yarışmamalıdır. Pressed feedback, `button` rolü, mevcut erişilebilirlik etiketi/ipucu, 44–48 dp hedef, açık/koyu tema, uzun skor/kâr değeri, büyük yazı ve dar ekran davranışı doğrulanmalıdır. Güncel maça gidilemeyen durumda bandın mevcut guard davranışı korunmalı ve yanıltıcı şevron gösterilmemelidir. Referans: sahibin 2026-08-24 tarihinde paylaştığı `Super Kararı Detayı` `Photo 1.jpg`. **Yerel batch (2026-08-24):** `SONUÇ` başlığına yalnız `currentMatchKey` varken görünen, accessibility ağacından gizli ikincil sağ şevron eklendi. Şevron ayrı Pressable değildir; mevcut tam outcome hücresi tek 44–48 dp hedef ve aynı `openCurrentMatch` route'u olarak kaldı. Android 15 emülatöründe skor/kâr hizası korundu ve banda dokunma `Maç Detayı`nı açtı. Resmî Mobile kalite kapısı geçti; fiziksel Xiaomi/sahip kabulü bekleniyor. | LOW | READY |
| NXT-OBS-125 | 2026-08-24 | Jinx'e karaktere özgü canlı animasyon ve uyku/uyanma dili kazandırılması | Sahip gözlemi: Jinx çoğu zaman sabit duruyor ve yaşayan bir karakter yerine ekrana yapıştırılmış bir görsel gibi hissediliyor. Jinx'e özgü hareket dili mekanik bir sürekli zıplama veya tekdüze loop olmamalıdır. Sakin durumda çok hafif nefes/hover ritmi, asimetrik ağırlık kayması, farklı aralıklı doğal göz kırpma, kısa bakış kaçırma ve seyrek teal enerji vurguları değişken zamanlamayla birleşmeli; karakter uzun süre heykel gibi kalmamalı ama içeriğin önüne de geçmemelidir. Dokunma, sürüklemeye başlama/bırakma, hızlı menü ve rehber balonu açma gibi doğrulanmış kullanıcı etkileşimlerinde Jinx kısa anticipation, yönsel takip, küçük overshoot ve kontrollü settle tepkileri vermeli; aynı hareket her olayda tekrarlanmamalıdır. Tepkiler uygulamanın gerçek durumunu yanlış yorumlayan sevinç/uyarı üretmemeli ve Super/Toto/model sonucuna yeni iş semantiği bağlamamalıdır. Hareketler tek bir açık animasyon durum makinesinden yönetilmeli; mevcut drag konumu, safe-area clamp'i, pager geçişleri, tutorial/discovery akışı ve aynı anda tek sürekli animasyon kuralı korunmalıdır. Uygulama arka plandayken veya Jinx görünür değilken animasyon durmalı; düşük seviye cihazda gereksiz JS timer/render yükü oluşturmamalı ve fiziksel Xiaomi'de akıcı görünmelidir. Reduced-motion açıkken sürekli salınım ve sıçrama kapanmalı, kişilik erişilebilir kısa durum değişimleriyle korunmalıdır. Yeni ifade frame'leri gerekirse özgün BTB futbol-orb kimliği, şeffaf alfa, açık/koyu tema ve IP-sınırları korunarak ayrı görsel üretim brief'iyle hazırlanmalıdır. Kabul; fiziksel telefonda en az 30 saniyelik idle gözlem, dokunma/sürükleme/menü tepkileri, art arda pager geçişleri, foreground-background dönüşü, açık/koyu tema ve reduced-motion karşılaştırması üzerinden sahibin görsel onayıyla yapılmalıdır. **Yerel batch (2026-08-24):** Mevcut özgün Jinx frame'leri korunarak tek native-driver nefes fazı çok hafif hover, asimetrik tilt/scale ve teal sparkle ritmini yönetiyor; düzensiz blink/double-blink/bakış/bob/wink repertuvarı daha kısa, jitter'lı aralıklarla çalışıyor. Test edilen `AMBIENT / MENU / DRAGGING / GUIDING / SUSPENDED` motion-state önceliği idle, drag, menü, rehber, discovery, background ve reduced-motion yarışını engelliyor; drag başlangıcı anticipation, bırakış küçük overshoot/settle taşıyor. Android 15 emülatöründe 1,2 saniye aralıklı Jinx crop'larında `3981` ve `3186` değişen örnek piksel ölçüldü; menü ifadesi, drag out/back ve settle fatal/React hatası olmadan geçti. Resmî Mobile kalite kapısı geçti. **Sahip düzeltmesi / yeniden açılış (2026-08-24):** Belirli bir kullanıcı etkileşimsizliği süresinden sonra Jinx bulunduğu yerde sabitlenip uyku durumuna geçmelidir. Uyurken ekran üzerinde gezinme, hover/bob veya dikkat çeken enerji hareketi olmamalı; kapalı göz/uyku ifadesi ve ancak karakterin uyuduğunu doğal biçimde anlatan çok hafif nefes hareketi kullanılmalıdır. Ekranın herhangi bir yerine yapılan ilk dokunuş Jinx'i kısa ve karaktere özgü bir uyanma tepkisiyle tekrar aktif duruma kaldırmalıdır. Bu dokunuş görünmez bir katman tarafından yutulmamalı; hedeflenen buton, kaydırma, pager veya diğer ekran eylemi normal biçimde devam etmelidir. Jinx'e dokunma, sürükleme, menü/rehber açma ve yeni route etkileşimi de uyku zamanlayıcısını sıfırlamalıdır. Uyku; `SLEEPING` olarak mevcut açık motion-state önceliğine eklenmeli, drag/menu/guiding/background/reduced-motion durumlarıyla yarışmamalı, arka planda timer/render yükü üretmemeli ve uygulama tekrar öne geldiğinde yanlış uyanma göstermemelidir. Reduced-motion'da uyku/uyanma anlamı sabit frame veya kısa opacity/ifade geçişiyle korunmalıdır. Kabul; fiziksel Xiaomi'de bekleyerek uykuya geçiş, ekranın farklı bölgelerine ilk dokunuşla uyanma ve dokunulan kontrolün aynı anda çalışması, tekrar uyuma, drag/menu, pager/scroll, background dönüşü, açık/koyu tema ve reduced-motion üzerinden doğrulanmalıdır. Önceki animasyon taslağı yeni uyku durumuyla yeniden kalite ve görsel doğrulamadan geçirilmelidir. **Uyku/uyanma yerel batch (2026-08-24):** Jinx 30 saniye etkileşimsizlikten sonra kapalı göz, çok hafif nefes ve küçük `zZ` ile bulunduğu yerde uyuyor; hover, sparkle ve mikro ifadeler duruyor. Kök ekranın pasif `onTouchStart` gözlemi ilk dokunuşta kısa yaylı uyanışı başlatıyor fakat responder olmadığı için hedef switch, pager, scroll veya buton aynı dokunuşla çalışmaya devam ediyor. `SLEEPING` ve `REACTING` öncelikleri drag/menu/guide/background/reduced-motion ile deterministik çözüldü; ağ güncellemesi uyuyan Jinx'i uyandırmıyor. Pure state/activity testleri ve Android 15'te 33 saniye uyku + Canlı switch'iyle aynı-dokunuş uyanma smoke'u geçti. | MEDIUM | READY |
| NXT-OBS-126 | 2026-08-24 | Günlük Super sonucuna göre Jinx ruh hâli, zengin/fakir mizahı ve bilgi repertuvarı | Sahip, Jinx'in Özet ekranında görünen günlük Super sonucunu karakterine yansıtmasını istiyor: sonuç artıdaysa mutlu ve oyuncu biçimde zengin, eksideyse mutsuz ve komik biçimde fakir görünmeli; gelişmiş animasyon, küçük şakalar ve gerçek bilgiler kullanıcıyı güldürüp etkilemelidir. **Tek veri kaynağı:** Jinx bağımsız bir kâr hesabı yapmamalı; kartta gösterilen aynı `superKpis.metricDate` ve seçili `superStarFilter` kovasının `profit`, `won`, `lost` değerlerinden saf bir `POSITIVE / NEGATIVE / EVEN / EMPTY / UNKNOWN` sunum durumu türetilmelidir. `profit > 0` pozitif, `< 0` negatif; sonuçlanmış karar varken `0` dengede, `won + lost === 0` henüz sonuç yok; yükleme/hata/eksik veya güvenilmez veri `UNKNOWN` olmalıdır. Filtre değişince Jinx ve görünür kart aynı kovayı aynı anda anlatmalı; stale/yanlış tarih veya bağımsız `todaySuperProfit` fallback'iyle duygu uydurulmamalıdır. **Animasyon yönetmenliği:** Sonuç ruh hâli, `NXT-OBS-125` hareket durumundan ayrı bir katman olmalı; `mood` (günlük sonuç) ile `motion` (ambient/sleeping/dragging/menu/guiding/suspended) çapraz ama deterministik biçimde birleşmelidir. Tam skeç 2–4 saniyelik anticipation → ana hareket → ikincil hareket/follow-through → kontrollü settle yapısında olmalı, sonsuz gösteriye dönüşmemelidir. Pozitifte Jinx kısa bir şaşkınlık sonrası minik zengin aksesuarına bürünebilir, bronz/altın parayı havaya atıp yakalayabilir, kendinden emin poz ve ölçülü ışıltıyla settle olabilir; negatifte omuzları düşebilir, cebini ters çevirip tek bozukluğun kaçışını izleyebilir veya eğri kâğıt tacını düzeltmeye çalışabilir. Mizah Jinx'in kendi talihine yönelmeli; kullanıcıyla, kayıpla veya ekonomik durumla alay etmemeli, ağlama/utandırma/karanlık çaresizlik ve bahis artırma çağrısı üretmemelidir. `EVEN` için cüzdanla ateşkes/omuz silkme, `EMPTY` için hesap bekleyen meraklı nötr ifade; `UNKNOWN` için mevcut nötr Jinx kullanılmalıdır. Yeni aksesuar/frame gerekiyorsa şeffaf alfa, 68dp okunabilirlik, açık/koyu tema, BTB teal-bronz kimliği ve özgün karakter sınırlarıyla ayrı varlık seti hazırlanmalıdır. **Şaka ve bilgi dili:** Balon yalnız doğrulanmış değerleri kısa ve dinamik biçimde söylemeli; örnek tonlar: pozitifte `Kravatı taktım: +{profit}. {won} galibiyet iş yaptı.`, negatifte `Cebimde yankı var: {profit}. Bugün top benden zengin.`, dengede `Cüzdanla ateşkes: 0.00.`, sonuç yokken `Hesap kapanmadı; ben çayı koydum.` Metin seçili yıldız eşiğini gerektiğinde belirtmeli, yatırım/bahis tavsiyesi, kesin gelecek iddiası, kullanıcıyı suçlama veya sahte istatistik üretmemelidir. Aynı cümle arka arkaya gelmemeli; küçük bir pozitif/negatif/nötr repertuvar deterministik shuffle-bag ile tüketilmelidir. **Tempo ve yarışlar:** Tam skeç ilk güvenilir günlük değer, işaret değişimi veya kullanıcının yıldız filtresiyle görünür sonucun değişmesi gibi anlamlı sınırda bir kez çalışmalı; her refetch/render'da tekrarlanmamalıdır. Aynı işaretteki küçük değer güncellemesi en fazla kısa mikro tepki vermeli. Otomatik balon ve discovery/tutorial aynı anda açılmamalı; mevcut kesinti kotası/cooldown ile koordine edilmeli. Jinx uyuyorsa ağ güncellemesi onu uyandırmamalı; yeni mood ilk kullanıcı uyanışında uygulanmalıdır. Drag/menu/guiding sırasında skeç kuyruğa alınmalı veya güvenle atlanmalı; pager, scroll, buton ve ilk dokunuş yutulmamalıdır. Otomatik haptik verilmemeli; reduced-motion'da aksesuar/ifade ve kısa metinle aynı anlam korunmalıdır. BFF/SAP/model/ROI eşikleri, Super seçimi, notification ve bahis davranışı değişmemeli; yeni polling yapılmadan mevcut React Query cache/hook'u paylaşılmalıdır. Kabul; artı/eksi/sıfır/sonuç-yok/hata, yıldız filtresi geçişi, aynı verinin refetch'i, gün değişimi, sleep/wake, drag/menu/tutorial yarışı, tekrar etmeyen şakalar, düşük seviye Android performansı, açık/koyu tema, ekran okuyucu ve reduced-motion senaryolarıyla fiziksel Xiaomi'de sahibin görsel onayını gerektirir. **Ruh hâli yerel batch (2026-08-24):** Jinx, yeni polling veya ikinci hesap olmadan Özet'in aynı React Query `superKpis.metricDate` verisini ve aynı seçili `superStarFilter` kovasını paylaşarak `POSITIVE / NEGATIVE / EVEN / EMPTY / UNKNOWN` durumuna geçiyor. Pozitif taç/altın, negatif boş cüzdan, denge ve sonuç-bekleme aksesuarları; anticipation → yay → follow-through → settle skeçi ve yalnız gerçek kâr/galibiyet/kayıp değerlerinden üretilen, arka arkaya tekrarlamayan kısa öz-mizah repertuvarı eklendi. Aynı gün/filtre/işaret refetch'i tam skeçi yinelemiyor; sleep, drag, menu, guide, discovery ve reduced-motion sahipliği korunuyor. Pure testler beş veri durumunu, imza/replay ve şaka tekrarını doğruladı; gerçek API'nin `EMPTY` durumunda saat aksesuarı ve `HESAP BEKLİYOR` balonu Android 15'te görüldü. Artı/eksi fiziksel Xiaomi görsel kabulü bekleniyor. | MEDIUM | READY |
| NXT-OBS-127 | 2026-08-24 | Baskı farkını mutlak değer gösterme | Sahip isteğiyle tüm Mobile kullanıcı yüzlerinde `baskı farkı` işaretli sayı yerine mutlak büyüklük olarak gösteriliyor. Canlı kartın görünür değeri ve ekran okuyucu metni, maç/Super detayındaki `PressureBalance` değeri ve Super karar saha metriği ortak `formatAbsolute` biçimleyicisini kullanıyor; örneğin `-6,0` artık `6,0`. Ev/deplasman yönü mevcut ev/uçak ikonu, renk ve açıklama üzerinden korunuyor. Ham `pressureDiff`, yön türetimi, BFF/SAP/model ve karar davranışı değişmedi. Biçimleyici testi, resmî kalite kapısı ve Android 15 gerçek API görsel smoke geçti. | LOW | READY |
| NXT-OBS-128 | 2026-08-24 | Canlı maç dakikalarını minimal progress bar ile gösterme | Canlı, devre arası ve tamamlanmış maç kartlarında dakika artık tarih kapsülüne sıkışmıyor; kart başlığının altında üç piksellik sade bir progress çizgisi ve sağında yalnız dakika etiketi (`76'`, devrede `45' · DEVRE`, maç sonunda `90' · MS`) gösteriliyor. Oran 90 dakikaya göre hesaplanıp taşmadan sınırlandırılıyor; 90+ gerçek dakika etikette korunuyor. Başlamamış maçta yanıltıcı boş bar yok. Yüzey `progressbar` rolü ve sayısal erişilebilirlik değeri taşıyor. Pure dakika/oran sınır testleri, resmî kalite kapısı ve Android 15 gerçek API smoke'unda `76'`/`50'` görsel doğrulaması geçti. | LOW | READY |
| NXT-OBS-129 | 2026-08-26 | Canlı/Fikstür switch etiketinin kapsam değişiminde sabit kalması | Sahip fiziksel Xiaomi gözlemi: Canlı ekranındaki switch açıkken `Canlı / 1 maç` doğru. Switch kapatılıp liste Fikstür kapsamına geçtiğinde chip hâlâ `Canlı / 1 maç` yazıyor, oysa altta `Sabitlenenler` (Lyon–Fenerbahçe, 26 Ağu 22:00) ve `Bugün · 13:30` (Daejeon Citi–Ulsan Hora) fikstür kartları listeleniyor; yani etiket ve sayı görünen içeriği yanlış anlatıyor. Beklenen: switch kapalıyken tanım `Fikstür` olmalı ve o kapsamın kendi maç sayısı yazılmalı; erişilebilirlik metni de aynı kapsamı söylemeli. Kaynak kanıtı: `app/(tabs)/live.tsx` içinde `LiveSwitch` sabit `Canlı` metnini ve her iki durumda da `count={tabCounts.LIVE}` değerini alıyor; `tabCounts.FIXTURE` aynı `useMemo` içinde zaten hesaplanıyor, bu yüzden yeni BFF/SAP alanı, yeni sorgu veya sözleşme değişikliği gerekmez. Yalnız sunum kapsamı: `scope` route parametresi, filtre mantığı, yıldız filtresi, Super seçimi ve model davranışı değişmemelidir. Kabul: fiziksel cihazda switch açık/kapalı geçişinde etiket, sayı ve ekran okuyucu metninin listelenen kapsamla eşleşmesi. **Uygulandı (2026-08-26 batch):** `LiveSwitch` artık `liveCount`/`fixtureCount` alıyor; switch kapalıyken etiket `Fikstür`, sayı `tabCounts.FIXTURE` ve erişilebilirlik metni `${label}, ${count} maç` oluyor. Yeni BFF/SAP alanı, sorgu veya sözleşme değişikliği gerekmedi; `scope` route parametresi, filtre mantığı ve model davranışı değişmedi. Not: yıldız (`STAR`) sekmesi etkinken switch zaten kapalı olduğu için chip `Fikstür` gösterir — bu switch'in kendi kapalı-konum anlamıdır ve bugünkü yanlış `Canlı` etiketinden kesin olarak daha doğrudur; ayrı bir STAR etiketi uydurulmadı. Fiziksel Xiaomi görsel kabulü bekleniyor. | MEDIUM | READY |
| NXT-OBS-130 | 2026-08-26 | Super karar günlüğü açık-filtre etiketinin kısaltılması | Sahip isteği: `Karar günlüğü` ekranındaki `Yalnız açık 0` chip etiketi yalnız `Açık` olarak gösterilsin; sayı mevcut biçimiyle korunur. Kaynak: `app/(tabs)/super.tsx:194` görünür metni ve `app/(tabs)/super.tsx:196` içindeki `accessibilityLabel="Yalnız açık kararları göster"`. Yalnız etiket metni değişir; filtre davranışı, açık karar sayımı, `OPEN` karar semantiği ve Super sonuç gösterimi değişmemelidir. Erişilebilirlik metni yeni etiketle tutarlı kalmalıdır. Kabul: fiziksel cihazda chip metninin `Açık <sayı>` görünmesi ve filtrenin aynı sonuç kümesini vermesi. **Uygulandı (2026-08-26 batch):** Görünür metin `Açık {openCount}`, erişilebilirlik metni `Açık kararları göster` oldu. Filtre davranışı, açık karar sayımı ve Super karar semantiği değişmedi. Fiziksel Xiaomi görsel kabulü bekleniyor. | LOW | READY |
| NXT-OBS-131 | 2026-08-26 | `SONUÇ` bölümündeki ok işaretinin skordan kopuk durması | Sahip fiziksel Xiaomi gözlemi: `Super Kararı Detayı` ekranında `SONUÇ` bölümünün dokunulabilir alanı `3 - 0 / biten skor` hücresidir, fakat bunu anlatan `chevron-right` oku bölüm başlığının en sağ kenarında duruyor ve skordan görsel olarak çok uzak kalıyor. Sahip okun skora daha yakın konumlanmasını istiyor. Kaynak kanıtı: `app/super/[key].tsx` içinde ok `outcomeHeading` görünümünde `SONUÇ` eyebrow'u ile birlikte render ediliyor ve `styles.outcomeHeading` (`app/super/[key].tsx:721`) `justifyContent: "space-between"` kullandığı için ok kartın sağ kenarına itiliyor; asıl `Pressable` ise bir alttaki `bandRow` içindeki skor hücresidir. Yalnız yerleşim/sunum kapsamı: ok yalnız `currentMatchKey` varken gösterilme koşulu, `openCurrentMatch` hedefi, `accessibilityLabel`/`accessibilityHint` metinleri, `KARAR ANI` bandı, kâr değeri ve Super karar semantiği değişmemelidir. Ok dekoratif kalmalı (`accessibilityElementsHidden`), yeni dokunma hedefi eklenmemelidir. Kabul: fiziksel cihazda okun skorun görsel grubuyla birlikte okunması, dokunma alanının aynı davranışı sürdürmesi ve açık/koyu temada hizanın bozulmaması. **Uygulandı (2026-08-26 batch):** `styles.outcomeHeading` içindeki `justifyContent: "space-between"` kaldırılıp `gap: spacing.xs` kullanıldı; ok artık kartın sağ kenarına itilmek yerine `SONUÇ` eyebrow'unun yanında, skorun görsel grubuyla aynı hizada duruyor. Ok dekoratif kaldı, `openCurrentMatch` hedefi ve erişilebilirlik metinleri değişmedi. Fiziksel Xiaomi görsel kabulü bekleniyor. **Sahip düzeltmesi (2026-08-26, fiziksel APK `b7c1158` üzerinde):** İlk uygulamada ok `SONUÇ` eyebrow'unun yanına alınmıştı; sahip okun eyebrow'un değil **skorun** yanında olmasını istiyor. Ok `outcomeHeading` içinden çıkarılıp `bandRow` içindeki `Pressable`'ın skor hücresine taşındı: yeni `outcomeScoreRow` satırında skor değeri ile ok yan yana duruyor, böylece işaret gerçekten dokunulabilir olan öğenin üstünde. Büyük skor tipografisinin yanında `iconSizes.inline` küçük kaldığı için `iconSizes.control` kullanıldı. `outcomeHeading` yeniden düz bir eyebrow oldu. Ok dekoratif kalmayı sürdürüyor (`accessibilityElementsHidden`), `openCurrentMatch` hedefi ve `Pressable`'ın erişilebilirlik metinleri değişmedi; yeni dokunma hedefi eklenmedi. Mobile kalite kapısı geçti. Yeni fiziksel APK ve görsel kabul bekleniyor. | LOW | READY |
| NXT-OBS-132 | 2026-08-26 | Jinx balon metni için BFF arkasında ücretsiz LLM sağlayıcısı | Sahip isteği: Jinx'in konuşma balonu metni deterministik repertuvarın yanında bir LLM tarafından üretilsin; ilk versiyon bir sonraki `mobile cutover start` batch'inde başlayacak. **Mimari kısıt:** sağlayıcı çağrısı yalnız `zbet-cap` Mobile BFF route'unda yapılır; API anahtarı env'de tutulur ve Expo istemcisine, kaynağa, log'a, handoff'a veya APK'ya hiçbir biçimde girmez. Mobile yalnız kendi BFF'ini çağırır, sağlayıcıyı doğrudan çağırmaz. **Sağlayıcı seçimi:** ücretsiz katmanı olan bir sağlayıcı (Gemini AI Studio, Groq, OpenRouter değerlendirildi; Anthropic API'nin ücretsiz katmanı yoktur). BFF'te tek bir adapter arkasına konur ki kota yetmezse sağlayıcı değişimi tek dosyalık olsun. **Veri sınırı:** ücretsiz katmanlar prompt'u ürün geliştirme/eğitim için kullanabildiğinden prompt'a yalnız ekranda zaten görünen türetilmiş değerler (`profit`, `won`, `lost`, seçili yıldız eşiği) girer; kullanıcı kimliği, SAP verisi, ham maç/oran tablosu, model ağırlığı ve eşikleri gönderilmez. **Sözleşme koruması (`NXT-OBS-126` aynen sürer):** LLM karar vermez, yalnız ifade kurar. Çıktı gösterilmeden önce doğrulanır — uzunluk sınırı, yasaklı ifade filtresi (yatırım/bahis tavsiyesi, kesin gelecek iddiası, kullanıcıyı suçlama) ve çıktıdaki sayıların girdi değerleriyle birebir eşleşmesi. Doğrulama başarısız olursa, timeout/kota/hata durumunda ve ağ yokken mevcut deterministik repertuvara düşülür. **Tempo:** gün + seçili filtre + sonuç işareti anahtarıyla BFF tarafında cache; her render/refetch'te çağrı yapılmaz, balon üretimi UI'ı bloklamaz ve Jinx uyuyorken (`SLEEPING`) çağrı tetiklenmez. **Varsayılan kapalı:** anahtar yokken özellik flag'i kapalıdır ve uygulama bugünkü davranışını aynen sürdürür; böylece batch dış bağımlılık olmadan yerel olarak inilebilir. **Sahip bağımlılığı:** ücretsiz katman hesabının açılması ve anahtarın env'e konması sahibe aittir; asistan hesap açmaz ve anahtar girmez. **Kapsam dışı:** Super seçimi, model ağırlıkları, Toto tahmini, notification davranışı, SAP/BFF iş mantığı ve commit/push/deploy — her biri ayrı onaydır. Kabul: anahtar yokken bugünkü metin korunur; anahtar varken yalnız doğrulanmış LLM metni gösterilir; sayı uyuşmazlığı, yasaklı ifade, kota/timeout fallback ve cache testleri geçer; fiziksel Xiaomi'de sahibin görsel kabulü alınır. **İlk versiyon uygulandı (2026-08-26 batch):** BFF tarafında `zbet-cap/srv/mobile-bff/jinx-quip.js` eklendi — Gemini adapter, mood anahtarlı TTL cache ve saf `validateQuip` doğrulayıcısı (uzunluk, tek satır, yasaklı ifade, ve çıktıdaki her sayının izinli kümede olması). `POST /v1/jinx/quip` route'u `router.js` içinde `requireRead` altında yayınlandı; `jinxQuipService` varsayılanı `createDisabledJinxQuipService`, `server.js`/`standalone-server.js` ise `createRuntimeJinxQuipService()` ile env'den kurar. `BTB_JINX_LLM_API_KEY` yoksa servis fail-closed kapalıdır ve uygulama bugünkü deterministik davranışını sürdürür. Mobile tarafında `src/mascot/jinx-quip-remote.ts` ikinci bir savunma katmanı olarak uzunluk/tek satır/sayı doğrulamasını tekrar yapıyor; `BtbMascotOverlay` metni mood sınırında bir kez prefetch ediyor, Jinx uyuyorken veya uygulama arka plandayken çağırmıyor ve balonu hiçbir zaman bekletmiyor — doğrulanmış metin gelmediyse mevcut repertuvar kullanılıyor. Prompt'a yalnız `kind`, `filter`, `metricDate`, `won`, `lost`, `profit` gider; kullanıcı kimliği, SAP verisi, ham oran tablosu ve model eşiği gitmez. Log'a yalnız ret sebebi kodu yazılır, aday metin ve anahtar yazılmaz. BFF 12 yeni test, Mobile 6 yeni test; resmî Mobile ve BFF kalite kapıları geçti. **Açık bağımlılık:** sahip ücretsiz katman hesabını açıp `BTB_JINX_LLM_API_KEY` değerini env'e koyana kadar özellik kapalıdır; anahtarlı gerçek çıktı ve fiziksel Xiaomi kabulü bekleniyor. **Canlı doğrulama (2026-08-26, sahip anahtarı):** Sahip Google AI Studio'da `BTBmobile` projesinde Free tier Gemini anahtarı oluşturup `BTB_JINX_LLM_API_KEY` User environment değişkenine kaydetti. Doğrulamada dört düzeltme çıktı ve uygulandı: (1) `BTB_JINX_LLM_*` değişkenleri `zbet-cap/scripts/start-mobile-bff.ps1` içindeki User→Process kopyalama listesinde yoktu, üçü eklendi — aksi hâlde anahtar BFF sürecine hiç ulaşmıyordu. (2) Varsayılan model `gemini-2.0-flash-lite` artık mevcut değil; `gemini-2.5-flash-lite` ise `no longer available to new users` HTTP 404 veriyor. Google'ın işaret ettiği `gemini-3.5-flash-lite` doğrulandı ve sabit varsayılan yapıldı (otomatik güncellenen `-latest` alias'ı bilinçli olarak seçilmedi). (3) İlk prompt diakritiksiz ASCII Türkçe yazıldığı için model bozuk Türkçe üretiyor ve `won`/`lost` değerlerini gol sanıyordu; prompt düzgün Türkçeye çevrildi ve bu değerlerin gol/şut/skor olmadığı açıkça yazıldı. (4) Model sayıları harfle yazınca rakam tabanlı doğrulayıcı devre dışı kalıyordu; `spelled-number` kuralı eklendi ve hem yasaklı ifade hem sayı kontrolü diakritikten bağımsız hâle getirildi (`foldDiacritics`), böylece diakritiksiz yazılmış bir aday kuralı atlayamıyor. Anahtarla canlı üretim `POSITIVE`, `NEGATIVE`, `EVEN` durumlarında doğrulanmış metin döndürdü ve cache tekrarı çalıştı; `EMPTY` durumu `sıfır` kelimesi yüzünden reddedilip deterministik repertuvara düştü — beklenen fallback davranışı. Log'a yalnız ret sebebi kodu yazıldı, aday metin ve anahtar yazılmadı. BFF testleri 15'e çıktı, Mobile guard testi eklendi, iki resmî kalite kapısı da geçti. **Açık:** çalışan BFF süreci (pid 24768, 2026-08-24 başlangıçlı) hem eski kodu hem anahtarsız env'i taşıyor; özelliğin gerçekten yayına girmesi için kontrollü restart ve ardından fiziksel Xiaomi kabulü gerekiyor. **Kontrollü restart (2026-08-26, sahip onayı):** Çalışan BFF süreci (pid 24768, 2026-08-24 10:35 başlangıçlı) durduruldu ve logon task'ın kendi argümanlarıyla (`ensure-mobile-bff.ps1 -Port 4004 -AuthMode pilot`) yeniden başlatıldı; auth modu, port ve notification davranışı değiştirilmedi. Restart öncesi local ve public health 200 idi, sonrasında `LocalReady=True` ve `PublicReady=True` döndü; yeni süreç pid 41300, açılış log'u temiz ve stderr boş. Yeni süreç düzenlenmiş kaynaktan açıldığı için `jinx-quip.js` ve yeni route canlıdır; `BTB_JINX_LLM_API_KEY` User kapsamında mevcut ve artık `start-mobile-bff.ps1` kopyalama listesinde. Not: çalışan sürecin environment bloğu doğrudan okunmadı ve `POST /v1/jinx/quip` auth gerektirdiği için uçtan uca istek pilot anahtarıyla denenmedi — servis düzeyindeki canlı üretim kanıtı yukarıdaki doğrudan çağrılardan gelir. Fiziksel Xiaomi'de balon metninin gerçekten değiştiği hâlâ doğrulanmadı. **Commit/push ve APK (2026-08-26, sahip onayı):** `zbet-cap` `04d2e26` (main) ve `zbet-mobile` `b7c1158` (master) push edildi; ilgisiz dosya commit'e girmedi ve `git diff --check` temizdi. Pushed SHA'dan `arm64-v8a` pilot APK üretildi: `btb-mobile-next-arm64-pilot-b7c1158.apk`, 51,57 MB, `sha256 539200C91C93996BE4375283DD6F7C585ACA3C809BDDD82F6850F5D8ED14E618`, imza `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c` — önceki APK'larla aynı parmak izi, dolayısıyla mevcut kurulumun üzerine kaldırmadan kurulur. Tek ABI: `arm64-v8a`. Secret taraması APK'nın 1321 girdisi açılarak yapıldı (ilk ham bayt taraması sıkıştırılmış bundle'ı göremediği için güvenilmezdi ve tekrarlandı): Gemini anahtar değeri, `generativelanguage.googleapis.com`, `gemini-3.5-flash-lite` ve `BTB_JINX_LLM_API_KEY` adı APK'da yok. `x-goog-api-key` dizesi `classes3.dex` içinde bulundu fakat değişiklik öncesi `e4ab0af` APK'sında da aynı yerde mevcut — paketlenmiş Google/Firebase SDK'sına ait, bu işten kaynaklanmıyor. Sanity kontrolü olarak `/v1/jinx/quip` JS bundle'da, `api.surklase.com` `app.config` içinde doğrulandı; yani istemci kodu gerçekten bu APK'da. Not: `EXPO_PUBLIC_MOBILE_AUTH_MODE` User kapsamında ve `.env`'de yoktu, build sürecine geçici olarak `pilot` verildi ve kalıcı yazılmadı. Prosedür adım 8 temizliği bilinçli olarak yapılmadı: yeni APK henüz fiziksel olarak doğrulanmadı, `e4ab0af` geri dönüş yolu olarak korunuyor. Fiziksel Xiaomi kurulumu ve dört maddenin görsel kabulü bekleniyor. **Prosedür adım 8 temizliği (2026-08-26, sahip onayı):** `.codex-artifacts` yalnız son fiziksel telefon `arm64` APK'sını tutuyor: `btb-mobile-next-arm64-pilot-b7c1158.apk`, `sha256 539200C9...ED14E618` (temizlik sonrası yeniden doğrulandı, değişmedi). Geri Dönüşüm Kutusu'na taşınan 18 dosya: önceki APK sürümleri `btb-mobile-next-arm64-pilot-e4ab0af.apk` ve `btb-mobile-next-arm64-cutover-07.apk` ile 2026-08-24 emülatör smoke'undan kalan 16 geçici ekran/log kanıtı (`*.png`, `btb-ui-current.xml`). Hiçbiri kalıcı silinmedi; hepsi geri alınabilir. Kaynak, test, handoff ve `docs/observation_archive` temizliğe dahil edilmedi ve dokunulmadı. Tutulan APK, sahibin fiziksel Xiaomi'sinde kurulu ve çalışır durumda olduğu için geri dönüş noktasıdır; `NXT-OBS-131` ok düzeltmesi (`2536efb`) bu APK'da **yoktur** ve sonraki build'de gelecektir. | MEDIUM | READY |
| NXT-OBS-133 | 2026-08-29 | Canlı yüzeylerde maçın en yüksek model skorlu BTB seçimi | Sahibin fiziksel Xiaomi isteği ve iki ekran görüntüsü (`Photo 1.jpg`, `Photo 2.jpg`): `Canlı maçlar` listesindeki `MatchCard` ile `Canlı Maç Detayı` hero/karar alanı, maç içinde mevcut kararlar arasından **sayısal model skoru en yüksek olan tek kararı** BTB seçimi olarak göstermelidir. Bu kural en yeni karar veya en yüksek yıldız/rating kuralı değildir; model skoru seçimi belirler, yıldız yalnız seçilen kararın kendi rating'idir. Seçim yapıldıktan sonra `selectedOdd`, rating, karar dakikası, karar nedeni, model skoru/güven, seçim oranı, aynı marketin güncel oranı, oran yönü ve ilgili erişilebilirlik metinleri aynı karar snapshot'ından gelmeli; farklı kararların alanları bir kartta birleştirilmemelidir. Maç akışındaki bütün tarihsel Super kararları kronolojik olarak korunur; yalnız kart/hero/`Karar özeti` için temsilci karar değişir. Kabul fixture'ı özellikle daha yeni fakat düşük skorlu bir karar ile daha eski fakat yüksek skorlu bir karar içermeli ve iki yüzeyin de yüksek skorlu kararı ve ona ait oran/özet alanlarını aynı göstermesini doğrulamalıdır; eşit model skorunda tüm yüzeylerin kullanacağı deterministik tie-break cutover'da kaynak sözleşmesiyle açıkça sabitlenmelidir. Mevcut Mobile sözleşmesinde `MatchSummary` seçim/rating/oranları taşır fakat model skorunu taşımaz; `MatchDetail.decisionScore` tek current-match özetidir, `SuperLog` listesi model skoru içermez ve `modelScore` yalnız `SuperLogDetail`'da vardır. Bu nedenle güvenli çözüm yalnız UI sıralaması değildir: `Mobile -> BFF -> OData/CDS/ABAP` zincirinde kanonik karar kaynağı doğrulanmalı, BFF iki yüzeye aynı atomik temsilci-decision DTO'sunu vermeli ve SAP alanı/aktivasyonu gerekiyorsa ayrı onay kapısı korunmalıdır. `NXT-OBS-117`'deki current-match/SuperLog snapshot ayrışması aynı tasarımda çözülmeli; yeni kural eski tarihsel kararları silerek veya model skorunu rating'den türeterek kapatılmamalıdır. **2026-08-29 cutover:** BFF, canlı/devre arası her maç için yayınlanmış `SuperLog.final_score` üzerinden en yüksek sayısal skorlu, void olmayan kararı seçiyor; eşitlikte en yeni `created_at`, sonra dakika/market/rating/reason sırası kullanılıyor. Seçim, rating, karar dakikası/nedeni/skoru/güveni ve seçim oranı tek Super Log satırından atomik taşınıyor; güncel oran seçilen marketten okunuyor. Live listesi ve Match Detail mevcut ortak DTO alanlarını kullandığı için Mobile bileşeni veya sözleşme şekli değişmedi; tarihsel timeline korundu. Daha yeni düşük skor/eski yüksek skor, eşitlik, void, skorsuz satır ve mevcut fallback regresyonları geçti. BFF test+production build, Mobile type/lint/test ve Expo Doctor yeşil. Canlı MCP yalnız `ZBET_UI_SUPER_LOG_SB` binding'inin published OData V4 olduğunu doğruladı; MCP ile satır okunduğu iddia edilmez. İşlevsel değişiklik `zbet-cap adc1822` olarak commit/push edildi ve temiz pushed kaynaktan pilot BFF bir kez yeniden başlatıldı. Local/public health HTTP 200; üç skorlu, void olmayan kararı bulunan canlı örnekte `3.89 / Ms25a / 74'` en yüksek karar olarak hem Live kartında hem Match Detail'de local/public atomik eşleşti. Mobile kaynak/sözleşme/native girdi değişmediği için APK build gerekmedi ve yapılmadı; yalnız fiziksel Xiaomi kabulü beklenir. Ayrıntı: `docs/observation_archive/cutover_2026-08-29.md`. | HIGH | READY |

| NXT-OBS-117 | 2026-08-22 | Güncel maç rating'i ile son Super karar rating'inin ayrışması | Fiziksel Xiaomi görsellerinde Avia Swidnik–Zawisza Bydgoszcz için hero `Ms35a · BTB rating 2/5` ve karar özeti `56′ karar` gösterirken timeline aynı marketin `47′ · 2 yıldız` ve daha yeni `56′ · 1 yıldız` kayıtlarını doğru biçimde gösteriyor. Bu `Ms35a` marketine özel bilinçli bir yıldız kuralı değildir. BFF alan zincirinde hero rating'i current-match `zbet_cds_005.Changed`, karar dakikası `last_super_elapsed`; timeline rating'i tarihsel `SuperLog.rating` kaynağından geliyor. Ekran anında `Changed=2` ile `last_super_elapsed=56` birleşmiş, fakat 56′ SuperLog rating'i 1; Mobile'ın exact `selectedOdd + rating + elapsed` güncel-karar eşlemesi de bu nedenle hiçbir timeline satırını `GÜNCEL` işaretleyemiyor. Gol sonrası 79′ fiziksel ekranında hero doğru biçimde `Aday bekleniyor / İzleniyor` durumuna geçti; 85′ public read-back de current selection'ın boş, rating'in `0` ve decisionMinute'ın `null` olduğunu, tarihçenin ise `47′/2` ve `56′/1` kaldığını doğruladı. Bu, önceki görünümün karar temizliği tamamlanmadan görülen geçici ve tutarsız bir snapshot olduğunu güçlendiriyor. Cutover'da UI yıldızını gizlemek yerine current-match/SuperLog source-of-truth ve atomik snapshot tutarlılığı çözülmeli; tarihsel kararlar korunmalıdır. **`mobile cutover start` batch değerlendirmesi (2026-08-22): blocker olarak bırakıldı, uygulanmadı.** Kökü Mobile'ın kendi eşleme mantığında değil, `current-match`/`SuperLog` kaynaklarının atomik olmayan anlık görüntüsünde; gerçek düzeltme BFF (`zbet-cap`) ve muhtemelen SAP (`zbet-abap`) tarafında source-of-truth/snapshot tutarlılığı gerektiriyor. Bu, tek bir Mobile-local dosya değişikliğiyle "yeterince açık" biçimde kapatılamayacak kadar geniş ve belirsiz bir mühendislik kararı; ayrı scoping ve (SAP tarafı etkileniyorsa) ayrı onay gerektirir. **2026-08-23 kapsam belirleme (kod uygulanmadı):** Kök neden `zbet_cl_main.clas.abap`'ta doğrulandı (satır ~2299-2391) — `insert_super_log` çağrısıyla aynı `TRY` bloğu içinde `cs_match-t_match-changed` güncelleniyor ve çağıran tarafından ayrı bir adımda `zbet_t_matches`'e persist ediliyor; `zbet_t_super_log` satırının yazılmasıyla bu persist iki ayrı, atomik olmayan yazma. `zbet_cds_005` zaten aynı `_LastSuperLog` join'inden `last_super_rating` (`super_last.rating`) alanını projekte ediyor (`last_super_elapsed`'i besleyen join ile aynı), ama BFF mapper (`zbet-cap/srv/mobile-bff/sap-mappers.js:542`) hero rating'i bunun yerine `matches.changed`'den okuyor. Kaynağı `last_super_rating`'e çevirmek CDS/DDIC değişikliği veya aktivasyon gerektirmeyen, yalnız `zbet-cap` tarafında bir aday çözüm olabilir — fakat bu yine "Super rating'in gösterim kaynağı" değişikliği olduğundan aktif M4 checkpoint'inin "model, threshold, rating, gate" freeze'i kapsamına girip girmediği sahip kararı gerektiriyor; sap-adt MCP bu oturumda `tool-visibility-unverified` döndürdüğü için `last_super_rating`'in çalışan OData'da canlı olduğu da doğrulanmadı. Sahip bu turda ilerletmemeyi tercih etti (aciliyet yok); blocker olarak açık kalıyor. **2026-08-24 cutover:** M4 dondurması korunarak Mobile/BFF/SAP rating kaynağına hiçbir değişiklik yapılmadı; madde ayrı sahip kararı ve canlı SAP kanıtı bekliyor. **2026-08-24 ikinci cutover:** Kanonik M4 otoritesi rating kaynağı değişikliğini 7 günlük checkpoint (`2026-08-27T00:48:51Z`) tamamlanana kadar açıkça yasakladığı için aday BFF eşlemesi uygulanmadı. Canlı SAP kanıtı bu kapıyı açamayacağından bu turda istenmedi; madde M4 sonrası BTB sahipliği ve canlı `last_super_rating` OData doğrulaması için `DEFERRED` durumuna alındı. Mobile/BFF/SAP kodu, runtime ve APK değişmedi. | HIGH | DEFERRED |
| NXT-OBS-118 | 2026-08-22 | Skor ilerlediği halde gol timeline'ının başarıyla boş dönmesi | Aynı fiziksel maç ekranında skor 79′ itibarıyla `1-3` olmasına rağmen akış kartı `Henüz gol veya kırmızı kart yok.` diyor. 85′ public read-back current match skorunu `1-3` ve durumu `LIVE` doğrularken Live Context `availability=OK`, `stale=false`, `refreshFailed=false`, `timeline=[]`, `goalCount=0` ve `latestGoalMinute=null` döndürdü; health telemetry son okumayı HTTP `200 / OK` gösterdi. Bu nedenle sorun unavailable/stale UI fallback'i değil, ana maç skoru ile provider/SAP Live Context olay kapsamının ayrışmasıdır. Live Context, SAP current-match akışından bağımsız ve yalnız betimleyici bir upstream'dir; production Super scoring bu timeline/eventSummary verisini okumaz. Bu yüzden boş gol geçmişi `NXT-OBS-117` yıldız/snapshot ayrışmasının doğrudan nedeni değildir; iki belirti ancak sağlayıcı zamanlaması veya upstream tutarsızlığı gibi henüz kanıtlanmamış ortak bir nedene sahip olabilir. **Mobile-taraf UI kısmı çözüldü (2026-08-22, `mobile cutover start` batch):** `MatchTimelineCard`'a `homeScore`/`awayScore` prop'ları eklendi (`app/match/[key].tsx`'ten geçiriliyor); `state === "EMPTY"` iken skor toplamı `> 0` ise her iki boş-durum mesajı da (`Bu maçta henüz gol veya kırmızı kart yok.` / `Henüz gol veya kırmızı kart yok.`) yerini `Skor ilerledi ama olay detayı henüz gelmedi.`'ye bırakıyor; skordan dakika/takım/oyuncu uydurulmadı, yalnız mevcut skor+timeline karşılaştırması kullanıldı. `npm run check` temiz geçti. **Çözülmedi, ayrı ve daha büyük bir konu:** provider/SAP tarafında gerçek event kapsamının neden geç/eksik geldiği araştırılmadı — bu Mobile-local bir düzeltmenin kapsamı dışında, backend/provider tarafı ayrı bir inceleme gerektiriyor. | HIGH | RESOLVED |
| NXT-OBS-120 | 2026-08-22 | Ana ve yerel sekmelerde referans uygulama parity'sinde kusursuz yatay kayma | İlk 2026-08-22 referansında hedef, sayfanın parmağı 1:1 izlediği ve iki komşu içeriğin yarı geçişte birlikte göründüğü SofaScore düzeyinde yatay geçişti. `PanResponder` kök nedeni kaldırılıp Expo SDK 57 uyumlu Gesture Handler + Reanimated worklet zinciri 2026-08-23 batch'inde uygulandı; otomatik, bundle, compile ve emülatör smoke kapıları geçti. **Sahip yeniden açma kararı (2026-08-24):** Paylaşılan 35,68 saniyelik yeni karşılaştırma videosunun ilk bölümü örnek uygulamayı, ikinci bölümü BTB'yi gösteriyor. Sahip mevcut BTB hissini final kabul etmiyor ve geçişlerin örnekteki kadar kusursuz olmasını istiyor. Kabul yalnız API değişimine değil fiziksel davranışa dayanacaktır: içerik hareket başlar başlamaz parmağı kesintisiz ve 1:1 izlemeli; mevcut ve komşu sayfa ara karelerde birlikte görünmeli; bırakma/geri dönme tek, kararlı bir eğriyle tamamlanmalı; boş frame, ani snap, çift animasyon, Jinx sıçraması, alt navigasyon oynaması veya dikey scroll kaybı olmamalıdır. Aynı kalite `Canlı/Fikstür` gibi yerel sekmelerde ve yıldız filtresi bulunan `Karar günlüğü`/ilgili ekranlarda da çalışmalıdır: yıldız filtresi açık bir sayfada yatay jest filtre tarafından yutulmamalı, mevcut yerel-sekme-önce/ana-sekme-sınır kuralına göre komşu sayfaya geçmelidir; yıldız dropdown seçenekleri kendiliğinden sekmeye dönüştürülmez. Tap/menü açma ve yatay swipe birbirini yanlış tetiklememelidir. Reduced-motion davranışı korunmalı; Android fiziksel cihazda yavaş sürükleme, hızlı flick, iptal, ilk/son sınır, yıldız filtresi üstünden başlayan jest, uzun liste render'ı ve art arda geçişler frame-by-frame doğrulanıp sahibin görsel kabulü alınmalıdır. Referans: `WhatsApp Video 2026-08-24 at 11.47.49 AM.mp4`; video içeriği yalnız görsel davranış kanıtıdır. **Yerel batch (2026-08-24):** Beş ana ekran Expo Router'ın native `react-native-pager-view` tab navigatorına taşındı; sahneler eager bağlı, alt menü pager dışında sabit ve reduced-motion açıkken swipe kapalı/tap geçişi anlık. Canlı ekranı `Canlı/Fikstür/Yıldız`, Super ekranı `Tümü/Yıldız` için ayrı native yerel pager kullanıyor; önceki ortak `Screen` pan handler'ı kaldırılarak çift animasyon yarışı kapatıldı, yalnız detail edge-back kaldı. Android 15 x86_64 emülatör kaydında yavaş ana swipe ara karesinde iki komşu sahne birlikte görünürken alt bar sabit kaldı; devam jestleri `Canlı -> Fikstür -> Yıldız` ve `Super Tümü -> Yıldız` sayfalarına yerleşti. Type/lint/413 test/brand, Android export ve x86_64 native build/kurulum geçti. Fiziksel Xiaomi frame-by-frame sahip kabulü bekleniyor. | HIGH | READY |
| NXT-OBS-119 | 2026-08-22 | Başlama saati geçmiş `NOT_STARTED` maçların Fikstür'de kalması | **Çözüldü (2026-08-22, `mobile cutover start` batch):** `src/utils/live-match-tabs.ts`'e `FIXTURE_STALE_TOLERANCE_MS` (90 dk) ve Europe/Istanbul kickoff hesaplama (`formatFixtureDateTime` ile aynı `+03:00` sabit-offset varsayımı) eklendi; `matchLiveTab`'ın `FIXTURE` dalı artık `NOT_STARTED` bir maçı yalnız kickoff'tan beri geçen süre toleransın altındaysa (veya kickoff hiç ayrıştırılamıyorsa, eski davranışı koruyarak) Fikstür'de tutuyor. Erteleme/iptal durumu uydurulmadı — aşan maçlar sadece Fikstür/Canlı listelerinden düşüyor, yeni bir statü metni icat edilmedi. `live-match-tabs.test.ts`'e toleransın hem içinde hem dışında kaldığı iki durumu doğrulayan yeni test eklendi (`NXT-OBS-119` adıyla). `npm run check` (tsc/eslint/415 test/brand) temiz geçti. Fiziksel cihaz doğrulaması yapılmadı; sahip onayı ve commit/push ayrı adımdır. | MEDIUM | RESOLVED |
| NXT-OBS-099 | 2026-08-18 | Live Context gerçek olay yolu — SAP köprüsü | **RESOLVED 2026-08-18.** `ZBET_CL_PROVIDER_BRIDGE`, SICF ve TVARVC akışı aktive edilip gerçek gol payload’ıyla uçtan uca doğrulandı. 2026-08-21’de görülen SICF dayanıklılık regresyonu bu tamamlanmış aktivasyon işinden ayrılarak `NXT-OBS-104` altında izlenir. | HIGH | RESOLVED |

| NXT-OBS-098 | 2026-08-17 | Gerçek gol/kırmızı kart verisiyle doğrulama | **KAPANDI (2026-08-18 release).** `REAL_GOAL_VALIDATION = PHYSICAL_PASS` — gerçek GOAL olayları fiziksel Xiaomi doğrulamasında görüldü; sunucu tarafında da uçtan uca kanıtlandı (canlı route HTTP 200, `btb.live-context.v2`, 3 gerçek gol, dizilişler yok, 4 sarı + 12 değişiklik + 2 bölüm işaretçisi `excludedByScope` ile dışlandı). `REAL_RED_CARD_VALIDATION = OWNER_ACCEPTED_PENDING_NATURAL_OBSERVATION` — kırmızı kart uygulaması doğrulanmış sözleşme, normalizer semantiği ve otomatik testler temelinde sahip tarafından kabul edildi. **Gerçek bir kırmızı kartın fiziksel olarak gözlendiği iddia edilmiyor**; bugüne dek hiçbir gerçek sağlayıcı yanıtında kırmızı kart görülmedi. Doğal bir kırmızı kart yalnız gözlemsel doğrulamadır ve release engeli değildir — bkz. `NXT-OBS-100`. | MEDIUM | CLOSED |

| NXT-OBS-097 | 2026-08-17 | Live Context ürün kapsamı daraltıldı (sahip kararı) | Yayınlanan sözleşme `btb.live-context.v2` ile **yalnız gol ve kırmızı kart** taşır. Sıradan sarı kart, oyuncu değişikliği, bölüm işaretçisi ve muhtelif anlatım kapsam dışıdır; `İlk 11 ve dizilişler` modülü bileşen/ekran/sözleşme/varsayılan düzen dahil tamamen kaldırıldı (mevcut kurulumlar için ek göç gerekmez — düzen uzlaştırma kanonik olmayan id'yi ilk okumada düşürür). Kırmızıda `DIRECT_RED`/`SECOND_YELLOW_RED` ayrımı korunur ve erişilebilirlik etiketinde okunur. Kendi kalesine gol yalnız sağlayıcı açıkça belirtirse korunur. Kart yalnız ihraç olumlu kanıtlanırsa yayımlanır; tanınmayan genel kart değeri `UNCLASSIFIED_CARD` olarak dışlanır ve ham değeri `diagnostics.unclassifiedCardValues` içinde saklanır (yanlış-pozitif kırmızı üretilmez). Kapsam dışı sınıflar `diagnostics.excludedByScope` ile sayılır; `unknownFeedTypes` yalnız anlaşılmayan feed tipleri içindir. `eventSummary` yalnız yayımlanan timeline'dan türetilir, **betimleyicidir ve model girdisi değildir**; üretim Super skorlaması ve gol/kırmızı kart mantığı değiştirilmedi. Emülatörde dört durum da doğrulandı (dolu, boş, bayat, kullanılamıyor). | HIGH | RESOLVED |

| NXT-OBS-096 | 2026-08-17 | Bilyoner Live Context erişim sınıflandırması | **SUPERSEDED.** İlk `curl/Node` ayrımı sonraki kontrollü ölçümle zamanlama tesadüfü olarak düzeltildi; kanonik çözüm ve gerçek aktivasyon `NXT-OBS-099`/`cutover_2026-08-18.md` içindedir. | HIGH | SUPERSEDED |

| NXT-OBS-095 | 2026-08-17 | Canlı maç fiziksel doğrulaması | **RESOLVED 2026-08-21.** Fiziksel Xiaomi görüntüsünde Minsk–Vitebsk 57′ canlı skor/dakika, Game Pulse, Maç Detayı ve 48′ Super kararı birlikte kararlı göründü. Public collector kanıtı `246/246` başarılı cycle, `0` failure, `464` canlı match observation ve güncel `lastCycleAt` gösterdi. Live Context/SICF dayanıklılığı bu UI/runtime doğrulamasından ayrı `NXT-OBS-104` kapsamıdır. | MEDIUM | RESOLVED |

| NXT-OBS-077 | 2026-08-11 | Bibi hedef vurgusu ve kapsamlı rehber | Rehber v2 Özet, Canlı, Super, Toto, Daha Fazla, Maç Detayı ve Super Detayı üzerindeki önemli kart/filtre/özet/sıralama alanlarını hedef kimliğiyle bağlar. Balon ölçülen hedefin üstüne veya altına yerleşir; adımlar kısa ve bağlamsaldır, ilerleme sürümlü saklanır ve tek yeniden başlat eylemi korunur. Android emülatöründe Özet üzerindeki üç ardışık hedef doğrulandı. | MEDIUM | READY |

| NXT-OBS-078 | 2026-08-11 | Detay ekranı edge-swipe navigasyonu | Maç, Super ve Toto detaylarında yalnız sol 32 dp kenardan başlayan yatay hareket etkinleştirildi; 76 dp/hız eşiği sonrası mevcut navigation stack ile geri döner, dikey kaydırmayı ve ana sekmeleri etkilemez. Android emülatöründe Maç Detayı → Özet kenar sürükleme dönüşü görsel olarak doğrulandı. | MEDIUM | READY |

| NXT-OBS-079 | 2026-08-11 | Opsiyonel açık tema | Koyu varsayılan korunarak aynı semantik token setinden tam açık palet üretildi. `Daha Fazla > Uygulama görünümü` seçimi güvenli depoda kalıcıdır ve kontrollü uygulama yenilemesiyle bütün ekranlara uygulanır. Android koyu/açık bundle ve açık tema release emülatör smoke’u geçti. | LOW | READY |

| NXT-OBS-080 | 2026-08-11 | Bibi hedef vurgusu regresyonu | Sabit koordinat ve tahminî dikdörtgenler kaldırıldı. `TutorialTarget` gerçek React Native bileşenini `measureInWindow` ile ölçer, yalnız aktif nesnenin sınırına 2 dp BTB yeşili çizer ve adım/ekran/kapanış değişiminde çerçeveyi temizler. Özet hero, Günlük Super ve ilk maç hedeflerinde ardışık emülatör ekran kanıtı alındı; eski büyük çerçeve özel durumu düzeltilip tekrar doğrulandı. Fiziksel telefon parity’si observation’da beklenir. | HIGH | READY |

| NXT-OBS-081 | 2026-08-11 | Açık tema kapsam netliği | Açık tema yalnız arka plan değil; yüzey, metin, sınır, durum, grafik, Bibi, sistem durum çubuğu, giriş/splash ve navigation renklerini ortak semantik paletten üretir. Giriş, Özet, Maç Detayı, rehber balonu ve hedef çerçeveleri açık tema release APK üzerinde görsel doğrulandı; seçim koyu varsayımı bozmadan kalıcıdır. | MEDIUM | READY |

| NXT-OBS-134 | 2026-08-29 | Canlı yüzeylerde BTB seçim önceliğini yıldız ve güncellik olarak değiştirme | **Sahip kural değişikliği; `NXT-OBS-133` kabul hedefini supersede eder.** `Canlı maçlar` kartı ile `Canlı Maç Detayı` temsilci BTB kararını artık en yüksek model skoruna göre seçmemelidir. Void/voided olmayan kararlar arasında önce en yüksek `rating`/BTB yıldızı kazanır; yalnız yıldızlar eşitse en yeni karar (`created_at`) gösterilir. Bu nedenle daha eski 3 yıldızlı karar, daha yeni 2 yıldızlı kararı yenmeli; iki 2 yıldızlı karar arasında daha yeni olan kazanmalıdır. Model skoru sıralama veya uygunluk filtresi değildir; yalnız seçilen kararın kendi model skoru alanı olarak taşınır ve başka kararın skoru/rating'i/oranı ile birleştirilmez. `selectedOdd`, rating, karar dakikası/nedeni/skoru/güveni, seçim oranı, seçilen marketin güncel oranı, oran yönü ve erişilebilirlik metinleri yine tek temsilci satırdan atomik gelmeli; tarihsel timeline korunmalıdır. Tam eşit `rating + created_at` durumunda kararsız sonuç üretmemek için kalan teknik tie-break sırası cutover'da açıkça sabitlenmelidir. Kabul regresyonları: eski yüksek-yıldız/yeni düşük-yıldız, eşit-yıldızda yeni karar, void satır, alan atomikliği, list/detail parity ve mevcut fallback. **2026-08-29 yerel batch:** BFF sırası `rating desc`, `created_at desc`, dakika, market ve neden olarak sabitlendi; model skoru sıralama/uygunluk filtresinden çıkarıldı, fakat seçilen satırın atomik bağımlı alanı olarak korundu. Eski yüksek-yıldız/yeni düşük-yıldız, eşit-yıldızda yenilik, void, skorsuz karar, list/detail parity, current-market ve fallback regresyonları geçti; production build yeşil. Pilot runtime hâlâ `adc1822` ve eski kuraldadır; commit/push ve restart yapılmadı. Ayrıntı: `docs/observation_archive/cutover_2026-08-29-02.md`. | HIGH | READY |

| NXT-OBS-135 | 2026-08-29 | Maç akışında Super yıldız göstergesini tek geniş düğümde birleştirme | Sahibin fiziksel Xiaomi görseli (`Photo 1.jpg`): Super timeline satırlarında rating'i gösteren soldaki ayrık küçük yıldızlar kaldırılmalıdır. Dikey akış eksenindeki mevcut konturlu büyük yıldız düğümü yatay yönde genişletilmeli ve kararın tam `rating` değeri (`1–5` yıldız) bu tek düğümün içinde gösterilmelidir; böylece aynı karar için biri merkezde, biri solda iki ayrı yıldız temsili kalmaz. Düğüm zaman çizgisinin merkezini, dakika kapsülünü, sağdaki market/`GÜNCEL`/şevron hizasını ve soldaki `SUPER` etiketini kaydırmamalı; 1–5 yıldız, dar Xiaomi genişliği, büyük yazı, açık/koyu tema ve reduced-motion durumlarında taşmamalıdır. Görsel yıldız sayısı ile ekran okuyucu `BTB rating n/5` anlamı aynı kaynaktan gelmeli; rating, model skoru, Super seçimi, timeline kronolojisi veya navigasyon davranışı değiştirilmemelidir. Kabul: ayrık küçük yıldızın tamamen yokluğu, geniş merkez düğümünde doğru 1/2/3/4/5 yıldız örnekleri, timeline merkez ve satır hizası, dokunulabilir karar satırı ve fiziksel Xiaomi görsel onayı. **2026-08-29 yerel batch:** soldaki ayrık rating yıldızı kaldırıldı; merkez düğüm yatay genişleyen tek 1–5 yıldız göstergesine dönüştürüldü ve görsel sayı ile `BTB rating n/5` erişilebilirlik metni ortak helper'dan üretildi. Typecheck/lint/unit, Expo Doctor'ın bağımsız patch-sürüm önerisi dışındaki kontroller ve Android production JS bundle geçti. Android 15 x86_64 emülatöründe pilot Match Detail açıldı; ayrık yıldızın kalktığı, merkez düğümün genişlediği ve satır hizasının korunduğu 1 yıldızlı canlı örnekte görsel doğrulandı; 1–5 sayımı unit testte kapsandı. Fiziksel Xiaomi kabulü ve yeni standalone APK ayrı kapıdadır. Ayrıntı: `docs/observation_archive/cutover_2026-08-29-02.md`. | MEDIUM | READY |

## 2026-08-21 cutover disposition (03)

- Komut: `btb next cutover start`. Freeze listesi `NXT-OBS-105`–`110`
  aralığındaki altı `OBSERVED` satırdı; batch sonunda açık `OBSERVED` kalmadı.
- `105`, current-match skor/dakikasını karar snapshot'ından ayıran Super sonuç
  bandıyla; `106`, yön uygunken yerel sekmeyi ana sekmeden önce tüketen tek
  seviyeli jestle; `107`, merkezi BTB maç akışıyla; `110`, özgün Jinx kimliği ve
  kullanıcı yüzeyi adlandırmasıyla yerel olarak tamamlandı ve `READY` oldu.
- `108` `DEFERRED`: Mobile DTO'sunda gerçek ikramiye/ödeme tutarı ve para birimi
  yok; `theoreticalPrize` kazanç kanıtı sayılmadı.
- `109` `DEFERRED`: market DTO'sunda doğrulanmış kickoff fiyatı yok;
  `RatioResult.kickOff` yüzde olduğundan fiyat gibi gösterilmedi.
- Mobile kalite kapısında TypeScript, ESLint, birim testleri ve marka kontrolü
  geçti. Expo Doctor'ın bağımlılık sürümü dışındaki kontrolleri geçti. Normal
  dependency check, batch'ten önceki `package.json`/lock baz çizgisinde dokuz
  Expo SDK 57 patch önerisi bildiriyor; kapsam dışı bağımlılık yükseltmesi
  yapılmadı.
- İzole `preview` profiliyle native prebuild ve tek `arm64-v8a` debug APK
  derlemesi geçti. Artefakt:
  `.codex-artifacts/btb-mobile-next-arm64-debug.apk`, SHA-256
  `0E5314080BAF284C5C73224125DE0EFE0A5B506D9233922E059B04E7A1943331`.
- Sonraki Android 15 x86_64 release emülatör smoke'unda `105` gerçek açık Super
  kararıyla `0-1 · 83' canlı`; `106` Canlı'da `Canlı -> Tümü -> Super` ve
  Super'da `Tümü -> Açık`; `107` üç gerçek gol, EV/DEP şeritleri, skor rozetleri
  ve `SON OLAY`; `110` koyu/açık Jinx, ayar metinleri, hızlı menü ve kapalı-göz
  frame'i ile geçti. Owner 2026-08-21'de aynı kapsamın ARM64 aday APK ile fiziksel
  Xiaomi testini tamamladı; `105`, `106`, `107` ve `110` parity kapısı kapandı.
- Geçici x86_64 release APK `53.919.417` bayttı; doğrulama sonrası ekran
  kanıtlarıyla birlikte Geri Dönüşüm Kutusu'na taşındı. Emülatörde güncel kurulum
  kaldı.
- Fiziksel telefon için yerel ARM64 release adayı üretildi:
  `.codex-artifacts/btb-mobile-next-arm64-cutover-03.apk` (`53.343.273` bayt;
  yalnız `arm64-v8a`; APK Signature Scheme v2 doğrulandı; SHA-256
  `E8A02ED307FC4455C70705883263DD6C276BA8D9E9285263875F747BD0E1443E`).
  Pilot debug sertifikasının SHA-256 özeti
  `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`.
  Owner aday APK'nın fiziksel Xiaomi testini 2026-08-21'de tamamlayıp Mobile
  parity'yi kabul etti. Harici APK dağıtımı yapılmadı.
- BFF/SAP/model sözleşmesi değiştirilmedi. Commit/push, deploy, dış sistem
  değişikliği, APK dağıtımı veya production signing yapılmadı.

## 2026-08-21 cutover disposition (02)

- Komut: `mobile cutover start` (kanonik akış: `btb next cutover start`). Freeze
  listesi sekiz `OBSERVED` satırdı: `NXT-OBS-073`, `074`, `086`, `089`, `095`,
  `096`, `099`, `100`.
- Batch sonunda açık `OBSERVED` satır kalmadı:
  - `073`, `074`, `086`, `089`, `100` doğru sahip/kanıt gelene kadar
    `DEFERRED`;
  - `095` fiziksel Xiaomi canlı maç kanıtıyla, `099` önceki gerçek SAP köprü
    aktivasyon kanıtıyla `RESOLVED`;
  - `096`, sonraki kontrollü teşhis tarafından geçersiz kılındığı için
    `SUPERSEDED`.
- `NXT-OBS-089` canlı maç bölümü fiziksel görüntüde geçti: Minsk–Vitebsk 57′,
  skor 0-1, Game Pulse ve `Seçim / Seçim oranı / Güncel oran` üçlüsü birlikte
  kararlıydı. Super kartındaki ayrı kâr/sonuç bandı kasıtlı tasarım olarak
  korundu; değiştirilmesi owner kararı gerektirir.
- `NXT-OBS-095` için aynı fiziksel görüntü canlı skor/dakika, Maç Detayı,
  Game Pulse ve 48′ Super kararını doğruladı. Public collector aynı anda
  `246/246` başarılı cycle, `0` failure, `464` canlı match observation ve güncel
  `lastCycleAt` yayımladı.
- SICF dayanıklılığı ayrı `NXT-OBS-104` kaydına alındı. İlk kontrolde
  `/sap/bc/zbet/provider/detail` 404 döndü; owner düğümü yeniden oluşturduktan
  sonra salt-okunur kontrol sırasıyla tokensız 401, tokenlı 200 ve BFF
  `availability=OK` gösterdi. Kapanış kontrolünde aynı uç yeniden 404 oldu.
  SAP yazma/aktivasyon yapılmadı. Yerel `zbet-abap` handler sınıfını içeriyor,
  fakat SICF nesnesi sürüm kontrollü değil; kalıcı düzeltme ayrı SAP ve abapGit
  onayı gerektirir.
- Yerel `zbet-cap` değişikliği pasif sağlık görünürlüğü ekledi:
  `liveContextTelemetry` yalnız gerçek maç çağrılarında zaten gözlenen son
  outcome, HTTP status ve güvenli sayaçları `/health` üzerinde yayımlar; ek
  upstream probe/isteği oluşturmaz.
- Doğrulama: hedefli BFF testleri `34/34`; resmi Mobile BFF kalite kapısında tüm
  testler ve production build geçti. Mobile kaynak değişmediği için yeni APK
  üretilmedi.
- Commit/push, deploy, SAP değişikliği veya artifact dağıtımı yapılmadı. Kapanış
  anında public runtime Live Context için dürüst biçimde 404/unavailable
  göstermektedir.

## 2026-08-21 cutover disposition

- `NXT-OBS-089` **kısmen** uygulandı, `OBSERVED` olarak kaldı çünkü madde iki
  ayrı talep içeriyordu ve yalnız biri çelişkisizdi.
  - **Canlı kart (uygulandı).** `app/match/[key].tsx` verdict satırı iki
    kolonluk dikey yığından (`seçim solda` / `seçim oranı + güncel oran sağda
    üst üste`) `Seçim / Seçim oranı / Güncel oran` üç kolonuna geçirildi;
    kolon isimlendirmesi ve hücre ritmi (`bandCell`/`bandCellWide`/
    `bandCellEnd`) Super detay ekranının zaten kurulu örüntüsünü birebir
    izler. Yön ikonu/renk semantiği (`movement.color`/`movement.icon`) ve
    market-kapalı/oran-bekleniyor durumları değişmeden taşındı; yön metni
    (`seçimden yükseldi` vb.) görünür etiketten kaldırılmadı, değer alanının
    `accessibilityLabel`'ına taşındı. `npm run typecheck`, `npm run lint`
    (`app/match/[key].tsx` kapsamlı) ve `npm test` (401/401) temiz geçti.
  - **Super kart (uygulanmadı — çelişki, blocker olarak raporlanıyor).**
    Madde metni "seçim, seçim oranı ve kâr aynı yatay metrik ritmine
    alınmalı" diyor; ancak `app/super/[key].tsx` içindeki mevcut kod bunun
    tam tersini kasıtlı olarak uyguluyor ve bunu satır içi yorumla
    gerekçelendiriyor: kâr (`profit`) karar anı bandından (`seçim` + `seçim
    oranı`) bilinçli olarak ayrı bir "sonuç" bandına alınmış, çünkü kârın
    karar satırıyla aynı sırada/ağırlıkta gösterilmesi önceki tasarımda
    "sonucun kararın bir parçasıymış gibi okunmasına" yol açıyordu. Madde
    aynı satırda "karar/biten skor ayrımı korunmalı" da diyor — bu ikisi
    birbiriyle doğrudan çelişiyor. Kod tarafındaki ayrım kasıtlı, belgeli bir
    tasarım kararı olduğu için varsayımla geri alınmadı; prosedürün
    "belirsiz iş kararı ... varsayımla uygulanmaz, blocker olarak raporlanır"
    kuralı burada işletildi. Karar sahibi gerekiyor: (a) mevcut iki-bant
    ayrımı doğru, madde metni bu kısımda güncellenip kapatılsın, ya da
    (b) kâr gerçekten seçim/seçim oranıyla aynı satıra alınsın ve mevcut
    tasarım yorumundaki gerekçe bilerek geri alınsın. Hiçbir kod değişikliği
    yapılmadı.
- Diğer 7 `OBSERVED` maddesi (`NXT-OBS-073`, `074`, `086`, `095`, `096`,
  `099`, `100`) bu batch'te kapsam dışı kaldı: `073`/`086` SAP DDIC/CDS veya
  Toto producer sahipliğinde ayrı onay gerektiriyor, `074` kök nedeni SAP
  producer/SM59 tarafında (Mobile kod değişikliği değil), `096`/`099` sahip
  kararı veya ayrı SAP aktivasyon onayı bekliyor, `095`/`100` kod değil
  fiziksel/gerçek-olay gözlemi bekliyor. Hiçbiri bu batch'in kapsamına
  alınmadı.
- Kod, `3396d4b428aea0e0c3f5952dbec31032f2189929` ile `zbet-mobile` `master`'a
  commit/push edildi (kullanıcı onayıyla).
- Doğrulama için yeni ARM64 pilot APK derlendi (kullanıcı onayıyla, canonical
  `scripts/build-pilot-apk.ps1` ile — Claude tarafında paralel bir build
  scripti yazılmadı):

```text
btb-mobile-next-arm64-verdict-columns.apk
SHA-256: 691CBA74505F4BE4E521498E54CF74C5CC20EF7E0A52749CCD4C1FC57113FC33
```

  İmza parmak izi bu shell'de `apksigner`/`keytool` ile doğrulanamadı
  (araç PATH'te bulunamadı); Android kurulum sırasında önceki APK'larla aynı
  imzayı taşımıyorsa kurulumu reddederek bunu kendisi doğrular. Fiziksel
  Xiaomi kurulumu ve üç-kolon verdict satırının görsel doğrulaması
  observation'da bekleniyor.

## 2026-08-18 Design V2 geri bildirim pass disposition

Fiziksel Xiaomi kullanımından çıkan dört maddelik sıra kapatıldı. Kod çalışma
ağacındadır ve **commit edilmemiştir**; commit/push ayrı açık onay gerektirir.
Kapı her adımda temiz: TypeScript, ESLint, `git diff --check` ve **387/387**
test (öncesi 342).

- **Phase D — paneller + kalıcılık.** `liveDetail` ve `superDetail` modülleri
  açılır/kapanır panel oldu. Kalıcılık `surface + kanonik modül id` ile ayrı bir
  depo anahtarında; saklanan değer kapalı kümedir, bu yüzden mevcut kurulum
  yükseltmede her şeyi açık bulur ve sonradan yayınlanan modül gizlenemez.
  Sıralama tercihi ve panel tercihi bağımsızdır. Hero yapısal olarak kapatılamaz.
- **Xiaomi canlı kart.** Alt metrik satırı yeniden tasarlanmadı; karar
  verilmemiş kartta yalnız gerçek değeri olan blok gösteriliyor, böylece üç ayrı
  `bekleniyor` etiketi tek dürüst ifadeye indi. Taşmaya karşı `flexWrap` /
  `minWidth: 0` / `flexShrink` savunması eklendi, `SuperLogCard` da tarandı.
- **Phase E.** Kodda kapatılamaz; `NXT-OBS-101` ve aşağıdaki
  `ASSET_GENERATION_DEPENDENCY` bölümüne bakınız.
- **Phase F/G.** Bibi tek seferlik mikro animasyon + uzun cooldown'lı yerel
  feature-discovery motoru. `LiveDot` ürünün tek sürekli ambient animasyonu
  olarak kaldı. `bibi-presence.ts` değiştirilmedi; Match Detail ve Super
  Decision Detail'de ambient Bibi yok. `Daha Fazla > Bibi ipuçları` ile
  `Normal` / `Sessiz`; `Sessiz` rehberi kapatmaz.

- **Super gün kapsamı varsayılanı (sahip isteği).** Karar günlüğü ilk açılışta
  `Bugün` kapsamında başlar. Kapsam route parametresindedir; kalıcı kullanıcı
  tercihi yoktur, bu yüzden ezilen bir tercih de yok. `Tüm günler` artık açık
  `scope=ALL` taşır, aksi hâlde seçilemez olurdu. `Bugün` yine en yeni maç günü
  demektir; backend, karar mantığı ve geçmiş veri değişmedi.

Fiziksel doğrulama bu maddelerin hiçbiri için **yapılmadı** — yeni ARM64 pilot
APK gerekiyor ve o da commit onayından sonra gelir.

## 2026-08-17 cutover disposition

- `NXT-OBS-090`, `091`, `092`, `093`, `094`: `mobile cutover start` bu maddeleri
  yeniden inceledi; kaynak kodda 2026-08-16 UX/etkileşim batch'inde zaten
  uygulandığı dosya/satır kanıtıyla doğrulandı (yeni kod değişikliği
  gerekmedi). Durumları `OBSERVED` -> `READY`; fiziksel cihazda madde bazlı
  ayrı doğrulama hâlâ bekleniyor (bugünkü fiziksel Xiaomi turu genel
  navigasyon/bildirim sağlığını ve Work Zone deep-link düzeltmesini
  doğruladı, bu beş maddeyi tek tek hedeflemedi).
- `NXT-OBS-073`, `074`, `086`: kapsam dışı bağımlılıklar nedeniyle
  değişmeden `OBSERVED` kaldı.
- `NXT-OBS-089`: kozmetik önceliklendirme nedeniyle değişmeden `OBSERVED`
  kaldı.
- Bu cutover ayrıca 2026-08-16 Work Zone migration'ının yol açtığı bir
  deep-link çift-hash regresyonunu buldu ve düzeltti (backlog'da önceden
  izlenen bir madde değildi). Ayrıntı, kod kanıtı ve fiziksel Xiaomi
  doğrulaması: `docs/observation_archive/cutover_2026-08-17.md`.

## 2026-08-13 cutover (02) disposition

- `NXT-OBS-089`, `091` ve `094`: yerel Mobile çözümü ve Android emülatör smoke'u
  tamamlandı; fiziksel ARM64 observation için `READY`.
- `NXT-OBS-090` ve `093`: Mobile + bounded BFF sözleşmesi/testi `READY`; public BFF
  yeni alanları ayrı BTP DEV deploy onayı verilene kadar yayınlamaz.
- `NXT-OBS-092`: tarihsel `last_pressure_*` değerinin güncelmiş gibi gösterilmesi
  kapatıldı ve güvenli null fallback `READY`; gerçek current-match pressure kaynağı
  SAP/OData alanı bulunmadığı için veri zenginleştirmesi `DEFERRED`.
- `NXT-OBS-073`, `074` ve `086`: ilgili upstream/operasyonel task blokajları nedeniyle
  açık kaldı; bu cutover sahte veri veya task sınırı aşan değişiklik yapmadı.

`READY`: çözüm yerel batch içinde uygulanmış ve otomatik/emülatör kapılarından
geçmiştir; fiziksel cihaz observation sonucu beklenir.

`DEFERRED`: doğru çözüm mevcut yerel Mobile/BFF sözleşmesinin dışındaki onaylı
bir bağımlılığı gerektirir; sahte veriyle kapatılmaz.

Durumlar: `OBSERVED`, `READY`, `DEFERRED`, `RESOLVED`.

Çözülen maddeler:

- `docs/observation_archive/cutover_2026-07-29.md`
- `docs/observation_archive/cutover_2026-07-29-03.md`
- `docs/observation_archive/cutover_2026-07-29-04.md`
- `docs/observation_archive/cutover_2026-08-01.md`
- `docs/observation_archive/cutover_2026-08-01-02.md`
- `docs/observation_archive/cutover_2026-08-01-03.md`
- `docs/observation_archive/cutover_2026-08-01-04.md`
- `docs/observation_archive/cutover_2026-08-04.md`
- `docs/observation_archive/cutover_2026-08-09.md`
- `docs/observation_archive/cutover_2026-08-09-02.md`
- `docs/observation_archive/cutover_2026-08-09-03.md`
- `docs/observation_archive/cutover_2026-08-09-04.md`
- `docs/observation_archive/cutover_2026-08-09-05.md`
- `docs/observation_archive/cutover_2026-08-11-02.md`
- `docs/observation_archive/cutover_2026-08-13-01.md`
- `docs/observation_archive/cutover_2026-08-17.md`

## ASSET_GENERATION_DEPENDENCY — kanonik BTB marka varlığı (2026-08-18)

Phase E izi tamamlandı. Bu bir kod kusuru değildir ve kodda kapatılamaz.

**Kanonik varlık.** Tek BTB marka raster'ı, bugün
`assets/brand/btb-mark.png` (192x192 RGBA). **Beş** yerde kullanılır — 2026-08-18
Milestone 2 ölçümü, önceki üç maddelik sayımın düzeltmesi:

```text
app.config.ts            icon
app.config.ts            android.adaptiveIcon.foregroundImage
AppLaunchScreen.tsx      uygulama içi açılış logosu          132 dp
app/(tabs)/index.tsx     Özet hero                            68 dp
TeamLogo.tsx             takım arması yoksa BTB fallback      40/24/18 dp
```

Uygulama ikonu ile uygulama içi logo **aynı kaynağı** paylaşır, yani tek dosya
değişince her yüzey birden değişir. Uygulama içi üç kullanım artık tek modülden
(`src/theme/brand-mark.ts`) geçer, config yolları da aynı sözleşmeden
(`src/theme/brand.ts`) okunur.

**En yüksek çözünürlüklü kaynak uygulamada değil.** `btb-assets/btb-logo.png`
(1254x1254, amblem + `BTB` + `BETTER THAN BET` kilidi) ve
`btb-assets/btb-logo-shell.png` (1024x1024, yalnız amblem) aynı markanın
ustalarıdır; uygulamanın 192px raster'ı ikincisinin küçültülmüşüdür (192px'e
indirgenmiş iki görüntü arasında ortalama luma farkı 0.74/255). `btb-assets`
hiçbir uygulama tarafından referans edilmiyor ve bu milestone'da
değiştirilmedi.

**Kare zemin nereden geliyor.** Raster'ın içinden. Ölçüm:

```text
assets/icon.png            192x192 RGBA · tam saydam piksel 0 · alfa 221..255
                           köşeler (0, 8, 27, α=221) — koyu lacivert, saydam değil
ic_launcher_foreground.webp 432x432 · tam saydam piksel 0 · köşeler (0, 8, 27, 221)
ic_launcher.webp            192x192 · tam saydam piksel 0
ic_launcher_round.webp      192x192 · tam saydam piksel 7951
```

Üçüncü satır belirleyici: APK'ya giren adaptive-icon **foreground** katmanı
kenardan kenara opak bir karedir. `mipmap-anydpi-v26/ic_launcher.xml` bir
`<background android:drawable="@color/iconBackground"/>` (`#04101E`) bildirir,
ama opak foreground onu tamamen örttüğü için bu arka plan hiç görünmez. Dördüncü
satır aynı sonucu tersten doğrular: yuvarlak varyantta saydam piksel vardır,
çünkü üretici aynı opak kareyi daire ile **kırpmıştır**. Launcher maskesi de bu
yüzden zemini değil, markanın kendi kenarlarını kırpar.

**Kodda ne yapıldı.** `AppLaunchScreen` logosundaki `borderRadius: 30` bir stil
tercihi değil, gömülü kareyi açılış gradyanı üzerinde gizleyen bir telafidir.
Kaldırılmadı — kaldırmak bugün görünür bir regresyon olurdu — ama gerçek varlık
geldiğinde aynı değişiklikte kaldırılması gerektiği kod içinde işaretlendi;
aksi hâlde artık maskelenmesi gerekmeyen bir çizimin köşelerini kırpar.

**Bağımlılık.** Gereken şey, şeffaf zeminli, gerçek anlamda yeniden tasarlanmış
BTB Intelligence Noir / Arcane-esinli özgün marka varlığıdır. Bu ortamda görsel
üretme/düzenleme yeteneği yoktur ve şunlar bilinçli olarak **yapılmamıştır**:
placeholder üretmek, mevcut logonun zeminini silmek, renklerini kaydırmak veya
mekanik olarak yeniden renklendirmek. Bunların hepsi handoff'ta açıkça
yasaklanmıştır ve hiçbiri marka sorununu çözmez.

Varlık geldiğinde beklenen biçim: adaptive-icon foreground kuralına uygun,
tam saydam zeminli, içeriği iç güvenli bölgede duran kare tuval. Takım
armalarına dokunulmadı.

**`assets/notification-icon.png` — silindi (2026-08-18).** Dosya marka
raster'ı ile **bayt bayt aynıydı** (`sha256` eşleşiyordu) ve hiçbir yerden
referans verilmiyordu. Bildirim küçük ikonu rolünü taşıdığı sanılabilirdi ama
taşımıyordu: o rol
`modules/btb-widget/android/src/main/res/drawable/btb_notification_icon.xml`
tek renkli vektörüne aittir ve `AndroidManifest.xml` içinde hem
`com.google.firebase.messaging.default_notification_icon` hem
`expo.modules.notifications.default_notification_icon` anahtarına kayıtlıdır.
Android küçük ikonu alfa maskesi olarak çizip rengi attığı için opak bir raster
zaten dolu bir kare verirdi — vektör doğru cevaptır. Ölü kopya kaldırıldı ve
`src/theme/brand.test.ts` bir raster'ın bu rolü geri almasını engelliyor.
`NXT-OBS-002` bu yüzden bildirim ikonu tarafında bir varlık bağımlılığı taşımaz.

**Doğrulayıcı (2026-08-18).** `npm run check:brand` verilen bir varlığı
sözleşmeye karşı ölçer: PNG biçimi ve 8-bit RGBA, kare tuval, en az 1024px,
gerçek saydam piksel, dört köşenin α=0 olması, rolüne göre içerik/tuval oranı,
optik merkez ve eski zeminden kalan yarı saydam hale oranı. Bugün **FAIL**
verir; mevcut varlığın gerçek durumu budur. Üretim brief'i:
`docs/ASSET_GENERATION_BRIEF.md`.
